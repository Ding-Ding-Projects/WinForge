<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.MinecraftWorldToolsModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xE7FC;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Pivot Grid.Row="2" x:Name="Tabs" Margin="0,4,0,0">
            <PivotItem x:Name="ChunkerTab">
                <ScrollViewer>
                    <StackPanel Spacing="12" Padding="0,4,8,4">
                        <InfoBar x:Name="ChunkerBar" IsClosable="True" IsOpen="False" />
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="10">
                                <TextBlock x:Name="ChunkerWorldLbl" FontWeight="SemiBold" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="WorldBox" Width="520" IsReadOnly="True" />
                                    <Button x:Name="PickWorldBtn" Click="PickWorld_Click" />
                                    <Button x:Name="OpenWorldBtn" Click="OpenWorld_Click" />
                                </StackPanel>
                                <TextBlock x:Name="WorldMetaText" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="10">
                                <TextBlock x:Name="ChunkerOutputLbl" FontWeight="SemiBold" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="ChunkerOutputBox" Width="520" />
                                    <Button x:Name="PickChunkerOutputBtn" Click="PickChunkerOutput_Click" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Name="PreviewBatchesBtn" Click="PreviewBatches_Click" />
                                    <Button x:Name="RunChunkerBtn" Click="RunChunker_Click" Style="{StaticResource AccentButtonStyle}" />
                                    <Button x:Name="CancelChunkerBtn" Click="CancelChunker_Click" IsEnabled="False" />
                                    <ProgressRing x:Name="ChunkerRing" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
                                </StackPanel>
                                <ListView x:Name="BatchList" MinHeight="120" MaxHeight="240" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem x:Name="BlueMapTab">
                <ScrollViewer>
                    <StackPanel Spacing="12" Padding="0,4,8,4">
                        <InfoBar x:Name="BlueMapBar" IsClosable="True" IsOpen="False" />
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <StackPanel Spacing="10">
                                <TextBlock x:Name="BlueMapOutputLbl" FontWeight="SemiBold" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="BlueMapOutputBox" Width="520" />
                                    <Button x:Name="PickBlueMapOutputBtn" Click="PickBlueMapOutput_Click" />
                                    <Button x:Name="OpenBlueMapOutputBtn" Click="OpenBlueMapOutput_Click" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Name="GenerateBlueMapConfigBtn" Click="GenerateBlueMapConfig_Click" />
                                    <Button x:Name="StartBlueMapBtn" Click="StartBlueMap_Click" Style="{StaticResource AccentButtonStyle}" />
                                    <Button x:Name="StopBlueMapBtn" Click="StopBlueMap_Click" />
                                    <ProgressRing x:Name="BlueMapRing" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem x:Name="SettingsTab">
                <ScrollViewer>
                    <StackPanel Spacing="12" Padding="0,4,8,4">
                        <InfoBar x:Name="SettingsBar" IsClosable="True" IsOpen="False" />
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                            <Grid ColumnSpacing="12" RowSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="WorkDirLbl" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="WorkDirBox" Grid.Row="0" Grid.Column="1" />
                                <Button x:Name="PickWorkDirBtn" Grid.Row="0" Grid.Column="2" Click="PickWorkDir_Click" />
                                <TextBlock x:Name="ChunkerToolLbl" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="ChunkerToolBox" Grid.Row="1" Grid.Column="1" />
                                <Button x:Name="PickChunkerToolBtn" Grid.Row="1" Grid.Column="2" Click="PickChunkerTool_Click" />
                                <TextBlock x:Name="ChunkerArgsLbl" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="ChunkerArgsBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" />
                                <TextBlock x:Name="ChunkerTargetLbl" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="ChunkerTargetBox" Grid.Row="3" Grid.Column="1" Width="220" HorizontalAlignment="Left" />
                                <TextBlock x:Name="ChunkerBatchLbl" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" />
                                <NumberBox x:Name="ChunkerBatchBox" Grid.Row="4" Grid.Column="1" Value="500" Minimum="64" Maximum="4096" SpinButtonPlacementMode="Inline" Width="180" HorizontalAlignment="Left" />
                                <TextBlock x:Name="BlueMapJarLbl" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="BlueMapJarBox" Grid.Row="5" Grid.Column="1" />
                                <Button x:Name="PickBlueMapJarBtn" Grid.Row="5" Grid.Column="2" Click="PickBlueMapJar_Click" />
                                <TextBlock x:Name="BlueMapArgsLbl" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" />
                                <TextBox x:Name="BlueMapArgsBox" Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" />
                                <TextBlock x:Name="BlueMapMemoryLbl" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" />
                                <NumberBox x:Name="BlueMapMemoryBox" Grid.Row="7" Grid.Column="1" Value="4096" Minimum="512" Maximum="131072" SpinButtonPlacementMode="Inline" Width="180" HorizontalAlignment="Left" />
                                <TextBlock x:Name="BlueMapThreadsLbl" Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" />
                                <NumberBox x:Name="BlueMapThreadsBox" Grid.Row="8" Grid.Column="1" Value="4" Minimum="1" Maximum="128" SpinButtonPlacementMode="Inline" Width="180" HorizontalAlignment="Left" />
                                <TextBlock x:Name="BlueMapPortLbl" Grid.Row="9" Grid.Column="0" VerticalAlignment="Center" />
                                <NumberBox x:Name="BlueMapPortBox" Grid.Row="9" Grid.Column="1" Value="8100" Minimum="1" Maximum="65535" SpinButtonPlacementMode="Inline" Width="180" HorizontalAlignment="Left" />
                                <ToggleSwitch x:Name="BlueMapWebToggle" Grid.Row="10" Grid.Column="1" />
                            </Grid>
                        </Border>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="SaveSettingsBtn" Click="SaveSettings_Click" Style="{StaticResource AccentButtonStyle}" />
                            <Button x:Name="ReloadSettingsBtn" Click="ReloadSettings_Click" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem x:Name="LogTab">
                <Grid RowSpacing="8" Padding="0,4,8,4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Button x:Name="ClearLogBtn" Click="ClearLog_Click" HorizontalAlignment="Right" />
                    <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                             FontFamily="Consolas" ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
