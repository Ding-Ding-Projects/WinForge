<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.SearchResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE721;" />

        <AutoSuggestBox x:Name="SearchBox" Grid.Row="1" QueryIcon="Find" TextChanged="SearchBox_TextChanged" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="10">
                <TextBlock x:Name="PagesLabel" FontWeight="SemiBold" FontSize="14" />
                <GridView x:Name="PagesGrid" IsItemClickEnabled="True" ItemClick="Pages_ItemClick" SelectionMode="None">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border Width="240" Padding="14,10,14,10" Margin="0,0,8,8"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="{Binding Glyph}" FontSize="18" VerticalAlignment="Center" />
                                    <StackPanel Spacing="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding En}" FontWeight="SemiBold" FontSize="13" />
                                        <TextBlock Text="{Binding Zh}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock
                                            Text="{Binding ReactorRequirementBadge}"
                                            FontSize="10"
                                            Opacity="{Binding ReactorRequirementOpacity}"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>

                <TextBlock x:Name="TweaksLabel" FontWeight="SemiBold" FontSize="14" Margin="0,6,0,0" />
                <StackPanel x:Name="TweaksPanel" Spacing="8" />

                <TextBlock x:Name="EmptyText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
