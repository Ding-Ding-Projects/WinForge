<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.VolumeMixerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE767;">
            <controls:ModuleHeader.ActionContent>
                <Button x:Name="RefreshBtn" Click="Refresh_Click" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock x:Name="RefreshText" />
                    </StackPanel>
                </Button>
            </controls:ModuleHeader.ActionContent>
        </controls:ModuleHeader>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <!-- Device picker row: choose which playback device the mixer shows + make it system default. -->
        <Grid Grid.Row="2" ColumnSpacing="12" Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="DeviceLabel" Grid.Column="0" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <ComboBox x:Name="DevicePicker" Grid.Column="1" HorizontalAlignment="Stretch"
                      SelectionChanged="DevicePicker_SelectionChanged" />
            <Button x:Name="SetDefaultBtn" Grid.Column="2" Click="SetDefault_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE73E;" FontSize="14" />
                    <TextBlock x:Name="SetDefaultText" />
                </StackPanel>
            </Button>
        </Grid>

        <InfoBar x:Name="HintBar" Grid.Row="3" IsClosable="True" IsOpen="False" Margin="0,2,0,0" />

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Root" Spacing="10" />
        </ScrollViewer>
    </Grid>
</Page>
