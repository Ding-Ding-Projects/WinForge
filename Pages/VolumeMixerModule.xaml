<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.VolumeMixerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE767;">
            <controls:ModuleHeader.ActionContent>
                <Button x:Name="RefreshBtn" Click="Refresh_Click" MinHeight="44" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock x:Name="RefreshText" />
                    </StackPanel>
                </Button>
            </controls:ModuleHeader.ActionContent>
        </controls:ModuleHeader>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <!-- Stacked controls keep long bilingual labels usable at narrow widths and high display scales. -->
        <StackPanel Grid.Row="2" Spacing="8" Margin="0,2,0,0">
            <TextBlock x:Name="DeviceLabel" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <ComboBox x:Name="DevicePicker" HorizontalAlignment="Stretch"
                      MinHeight="48" SelectionChanged="DevicePicker_SelectionChanged" />
            <Button x:Name="SetDefaultBtn" Click="SetDefault_Click" HorizontalAlignment="Stretch" MinHeight="48">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE73E;" FontSize="14" />
                    <TextBlock x:Name="SetDefaultText" Grid.Column="1" TextWrapping="Wrap" />
                </Grid>
            </Button>
        </StackPanel>

        <InfoBar x:Name="HintBar" Grid.Row="3" IsClosable="True" IsOpen="False" Margin="0,2,0,0" />

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Root" Spacing="10" />
        </ScrollViewer>
    </Grid>
</Page>
