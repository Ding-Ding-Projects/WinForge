<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.VisualStudioInstallerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE70C;" />
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <InfoBar x:Name="ResultBar" Grid.Row="2" Margin="0,8,0,0" IsClosable="True" IsOpen="False" />

        <ScrollViewer Grid.Row="3" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Root" Spacing="14" />
        </ScrollViewer>
    </Grid>
</Page>
