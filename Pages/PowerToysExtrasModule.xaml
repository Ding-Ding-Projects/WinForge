<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.PowerToysExtrasModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE945;" />
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Pivot x:Name="Tabs" Grid.Row="2">

            <!-- ===== Image Resizer ===== -->
            <PivotItem x:Name="TabResize">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="12" Padding="0,8,0,0">
                        <TextBlock x:Name="ResizeIntro" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="ResizeAddBtn" Click="ResizeAdd_Click" />
                            <Button x:Name="ResizeClearBtn" Click="ResizeClear_Click" />
                            <TextBlock x:Name="ResizeCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Height="150">
                            <ListView x:Name="ResizeList" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontSize="12" TextTrimming="CharacterEllipsis" Padding="4,2,4,2" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>

                        <Grid ColumnSpacing="12" RowSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Spacing="4">
                                <TextBlock x:Name="PresetLabel" FontSize="12" />
                                <ComboBox x:Name="PresetBox" HorizontalAlignment="Stretch" SelectionChanged="Preset_Changed" />
                            </StackPanel>
                            <CheckBox x:Name="ShrinkOnlyChk" Grid.Row="0" Grid.Column="1" VerticalAlignment="Bottom" />

                            <StackPanel Grid.Row="1" Grid.Column="0" Spacing="4">
                                <TextBlock x:Name="WidthLabel" FontSize="12" />
                                <NumberBox x:Name="WidthBox" Value="1920" Minimum="1" Maximum="20000" SpinButtonPlacementMode="Inline" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Spacing="4">
                                <TextBlock x:Name="HeightLabel" FontSize="12" />
                                <NumberBox x:Name="HeightBox" Value="1080" Minimum="1" Maximum="20000" SpinButtonPlacementMode="Inline" />
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Spacing="4">
                                <TextBlock x:Name="QualityLabel" FontSize="12" />
                                <NumberBox x:Name="QualityBox" Value="90" Minimum="1" Maximum="100" SpinButtonPlacementMode="Inline" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="1" Spacing="4">
                                <TextBlock x:Name="SuffixLabel" FontSize="12" />
                                <TextBox x:Name="SuffixBox" Text="-resized" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Spacing="4">
                            <TextBlock x:Name="OutFolderLabel" FontSize="12" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBox x:Name="OutFolderBox" Width="380" IsReadOnly="True" />
                                <Button x:Name="OutFolderBtn" Click="OutFolder_Click" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="ResizeRunBtn" Click="ResizeRun_Click" Style="{StaticResource AccentButtonStyle}" />
                            <ProgressRing x:Name="ResizeRing" IsActive="False" Width="20" Height="20" />
                            <TextBlock x:Name="ResizeProgressText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <!-- ===== Text Extractor (OCR) ===== -->
            <PivotItem x:Name="TabOcr">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="12" Padding="0,8,0,0">
                        <TextBlock x:Name="OcrIntro" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <StackPanel Spacing="4">
                            <TextBlock x:Name="OcrLangLabel" FontSize="12" />
                            <ComboBox x:Name="OcrLangBox" HorizontalAlignment="Stretch" DisplayMemberPath="DisplayName" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="OcrFullBtn" Click="OcrFull_Click" Style="{StaticResource AccentButtonStyle}" />
                            <ProgressRing x:Name="OcrRing" IsActive="False" Width="20" Height="20" />
                            <TextBlock x:Name="OcrHint" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="OcrCopyBtn" Click="OcrCopy_Click" />
                            <Button x:Name="OcrClearBtn" Click="OcrClear_Click" />
                        </StackPanel>

                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Height="220">
                            <TextBox x:Name="OcrResult" AcceptsReturn="True" TextWrapping="Wrap"
                                     IsSpellCheckEnabled="False" FontFamily="Consolas"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0" Background="Transparent" />
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <!-- ===== Always On Top ===== -->
            <PivotItem x:Name="TabTop">
                <Grid RowSpacing="10" Padding="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="TopIntro" Grid.Row="0" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
                        <Button x:Name="TopRefreshBtn" Click="TopRefresh_Click" />
                        <Button x:Name="TopUnpinAllBtn" Click="TopUnpinAll_Click" />
                        <TextBlock x:Name="TopCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <Border
                        Grid.Row="2"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8">
                        <ListView x:Name="TopList" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="6,5,6,5" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Process}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                        </StackPanel>
                                        <ToggleButton Grid.Column="1" Content="{Binding PinLabel}" Tag="{Binding}" Click="TopPin_Click" IsChecked="{Binding Pinned}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>
            </PivotItem>

            <!-- ===== Paste as Plain Text ===== -->
            <PivotItem x:Name="TabPaste">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="14" Padding="0,8,0,0">
                        <TextBlock x:Name="PasteIntro" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="14">
                            <StackPanel Spacing="6">
                                <TextBlock x:Name="PasteStripTitle" FontWeight="SemiBold" />
                                <TextBlock x:Name="PasteStripBlurb" TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <Button x:Name="PasteStripBtn" Click="PasteStrip_Click" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="14">
                            <StackPanel Spacing="8">
                                <TextBlock x:Name="PasteHotkeyTitle" FontWeight="SemiBold" />
                                <TextBlock x:Name="PasteHotkeyBlurb" TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="PasteHotkeySwitch" Toggled="PasteHotkey_Toggled" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <!-- ===== More PowerToys ===== -->
            <PivotItem x:Name="TabMore">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="12" Padding="0,8,0,0">
                        <TextBlock
                            Text="These PowerToys-style utilities already have their own native WinForge pages. Open them here so the whole suite stays discoverable from one hub. · 呢啲 PowerToys 式工具已經有自己嘅原生 WinForge 頁面。喺呢度直接開返，等成套工具可以由一個總入口搵到。"
                            TextWrapping="Wrap"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <Grid ColumnSpacing="10" RowSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button Grid.Row="0" Grid.Column="0" Content="Open Screen Ruler · 開啟螢幕間尺" Click="MoreScreenRuler_Click" />
                            <Button Grid.Row="0" Grid.Column="1" Content="Open Mouse Utilities · 開啟滑鼠工具" Click="MoreMouseUtils_Click" />
                            <Button Grid.Row="1" Grid.Column="0" Content="Open Quick Accent · 開啟快速重音符" Click="MoreQuickAccent_Click" />
                            <Button Grid.Row="1" Grid.Column="1" Content="Open Shortcut Guide · 開啟快捷鍵指南" Click="MoreShortcutGuide_Click" />
                            <Button Grid.Row="2" Grid.Column="0" Content="Open Text Extractor (OCR) · 開啟文字擷取（OCR）" Click="MoreTextOcr_Click" />
                            <Button Grid.Row="2" Grid.Column="1" Content="Open Awake · 開啟保持喚醒" Click="MoreAwake_Click" />
                            <Button Grid.Row="3" Grid.Column="0" Content="Open Color Picker · 開啟螢幕取色" Click="MoreColorPicker_Click" />
                            <Button Grid.Row="3" Grid.Column="1" Content="Open Peek · 開啟快速預覽" Click="MorePeek_Click" />
                            <Button Grid.Row="4" Grid.Column="0" Content="Open Workspaces · 開啟工作區" Click="MoreWorkspaces_Click" />
                            <Button Grid.Row="4" Grid.Column="1" Content="Open FancyZones · 開啟視窗分區" Click="MoreFancyZones_Click" />
                            <Button Grid.Row="5" Grid.Column="0" Content="Open New+ · 開啟範本新增" Click="MoreNewPlus_Click" />
                            <Button Grid.Row="5" Grid.Column="1" Content="Open Advanced Paste · 開啟進階貼上" Click="MoreAdvancedPaste_Click" />
                            <Button Grid.Row="6" Grid.Column="0" Content="Open Command Not Found · 開啟搵唔到指令" Click="MoreCmdNotFound_Click" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>

        <InfoBar x:Name="ResultBar" Grid.Row="3" IsClosable="True" IsOpen="False" />
    </Grid>
</Page>
