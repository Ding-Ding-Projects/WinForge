<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="WinForge.Pages.MailComposeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonClick="Send_Click">

    <Grid Width="560" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar x:Name="Bar" Grid.Row="0" IsClosable="True" IsOpen="False" />
        <TextBlock x:Name="FromLine" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
        <TextBox x:Name="ToBox" Grid.Row="2" />
        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="CcBox" Grid.Column="0" />
            <TextBox x:Name="BccBox" Grid.Column="1" />
        </Grid>
        <TextBox x:Name="SubjectBox" Grid.Row="4" />

        <StackPanel Grid.Row="5" Spacing="6">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="AttachBtn" Click="Attach_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontSize="13" Glyph="&#xE723;" />
                        <TextBlock x:Name="AttachLabel" />
                    </StackPanel>
                </Button>
                <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
            </StackPanel>
            <ItemsControl x:Name="AttachList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>

        <TextBox x:Name="BodyBox" Grid.Row="6" AcceptsReturn="True" TextWrapping="Wrap"
                 MinHeight="200" Height="240" VerticalContentAlignment="Top"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</ContentDialog>
