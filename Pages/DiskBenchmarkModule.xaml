<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.DiskBenchmarkModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xEC4A;" />
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <InfoBar x:Name="WarnBar" Grid.Row="2" IsClosable="False" IsOpen="True" Severity="Warning" />

        <!-- Controls -->
        <Grid Grid.Row="3" ColumnSpacing="8" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox x:Name="FolderBox" Grid.Row="0" Grid.Column="0" IsReadOnly="True" VerticalAlignment="Bottom" />
            <Button x:Name="BrowseBtn" Grid.Row="0" Grid.Column="1" Click="Browse_Click" VerticalAlignment="Bottom" />
            <ComboBox x:Name="DriveBox" Grid.Row="0" Grid.Column="2" MinWidth="180" SelectionChanged="Drive_Changed" VerticalAlignment="Bottom" />

            <ComboBox x:Name="SizeBox" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" MinWidth="180" />
            <ComboBox x:Name="PassBox" Grid.Row="1" Grid.Column="1" MinWidth="120" />
            <Button x:Name="RunBtn" Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Click="Run_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" />
        </Grid>

        <!-- Live progress -->
        <Grid Grid.Row="4" ColumnSpacing="12" Visibility="Collapsed" x:Name="ProgressRow">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressRing x:Name="Ring" Grid.Column="0" IsActive="True" Width="24" Height="24" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                <TextBlock x:Name="ProgressLabel" />
                <ProgressBar x:Name="ProgressBarCtl" Minimum="0" Maximum="1" />
            </StackPanel>
            <Button x:Name="CancelBtn" Grid.Column="2" Click="Cancel_Click" VerticalAlignment="Center" />
        </Grid>

        <!-- Results table -->
        <Border
            Grid.Row="5"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <ListView x:Name="ResultsList" SelectionMode="None">
                    <ListView.HeaderTemplate>
                        <DataTemplate>
                            <Grid Padding="12,8,12,8" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="140" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding}" FontWeight="SemiBold" />
                            </Grid>
                        </DataTemplate>
                    </ListView.HeaderTemplate>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4,8,4,8" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="140" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding MBpsText}" VerticalAlignment="Center" HorizontalTextAlignment="Right" FontFamily="Consolas" />
                                <TextBlock Grid.Column="2" Text="{Binding IopsText}" VerticalAlignment="Center" HorizontalTextAlignment="Right" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontFamily="Consolas" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="EmptyHint" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                           TextAlignment="Center" MaxWidth="360" />
            </Grid>
        </Border>

        <InfoBar x:Name="ResultBar" Grid.Row="6" IsClosable="True" IsOpen="False" />
    </Grid>
</Page>
