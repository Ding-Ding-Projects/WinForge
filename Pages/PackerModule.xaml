<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.PackerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,16,24,24" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="1040" Spacing="14">

            <!-- Header -->
            <controls:ModuleHeader x:Name="Header" Glyph="&#xE7B8;">
                <controls:ModuleHeader.ActionContent>
                    <Border
                        x:Name="VersionPill"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        CornerRadius="10"
                        Visibility="Collapsed">
                        <TextBlock x:Name="VersionText" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Border>
                </controls:ModuleHeader.ActionContent>
            </controls:ModuleHeader>
            <TextBlock
                x:Name="HeaderBlurb"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <InfoBar x:Name="EngineBar" IsClosable="False" IsOpen="False" Severity="Warning" />

            <!-- ===== Working folder ===== -->
            <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="FolderHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="PickFolderBtn" Click="PickFolder_Click" />
                        <Button x:Name="OpenFolderBtn" Click="OpenFolder_Click" />
                        <Button x:Name="RefreshBtn" Click="Refresh_Click" />
                    </StackPanel>
                    <TextBlock
                        x:Name="FolderPath"
                        FontFamily="Consolas"
                        FontSize="12"
                        IsTextSelectionEnabled="True"
                        TextWrapping="Wrap"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock x:Name="TemplatesLabel" FontWeight="SemiBold" FontSize="13" />
                            <ListView
                                x:Name="TemplatesList"
                                MaxHeight="160"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                SelectionChanged="Templates_SelectionChanged"
                                SelectionMode="Single">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock FontFamily="Consolas" FontSize="12" Text="{Binding}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <TextBlock x:Name="NoTemplates" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock x:Name="TargetsLabel" FontWeight="SemiBold" FontSize="13" />
                            <ListView
                                x:Name="TargetsList"
                                MaxHeight="160"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                SelectionMode="Multiple">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock FontFamily="Consolas" FontSize="12" Text="{Binding}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <RadioButton x:Name="TargetOnly" GroupName="targetMode" IsChecked="True" />
                                <RadioButton x:Name="TargetExcept" GroupName="targetMode" />
                                <Button x:Name="InspectTargetsBtn" Click="InspectTargets_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ===== Variables ===== -->
            <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="VarsHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Name="VarsBlurb" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

                    <!-- key/value editor -->
                    <StackPanel x:Name="VarsPanel" Spacing="6" />
                    <Button x:Name="AddVarBtn" Click="AddVar_Click" HorizontalAlignment="Left" />

                    <!-- var-files -->
                    <TextBlock x:Name="VarFilesLabel" FontWeight="SemiBold" FontSize="13" />
                    <ListView
                        x:Name="VarFilesList"
                        MaxHeight="120"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        SelectionMode="Multiple">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock FontFamily="Consolas" FontSize="12" Text="{Binding}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="AddVarFileBtn" Click="AddVarFile_Click" />
                        <Button x:Name="RefreshVarFilesBtn" Click="RefreshVarFiles_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== Run commands + console ===== -->
            <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="RunHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="InitBtn" Click="Init_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="ValidateBtn" Click="Validate_Click" />
                        <Button x:Name="FmtBtn" Click="Fmt_Click" />
                        <Button x:Name="BuildBtn" Click="Build_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="CancelBtn" Click="Cancel_Click" IsEnabled="False" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ProgressRing x:Name="BusyRing" Width="18" Height="18" IsActive="False" />
                        <TextBlock x:Name="StatusText" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>

                    <TextBlock x:Name="ConsoleLabel" FontWeight="SemiBold" FontSize="13" />
                    <Border Background="#1E1E1E" CornerRadius="6" Padding="0">
                        <ScrollViewer x:Name="ConsoleScroll" MaxHeight="320" VerticalScrollBarVisibility="Auto" Padding="12">
                            <TextBlock
                                x:Name="ConsoleText"
                                FontFamily="Consolas"
                                FontSize="12"
                                Foreground="#D4D4D4"
                                IsTextSelectionEnabled="True"
                                TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="ClearConsoleBtn" Click="ClearConsole_Click" />
                        <Button x:Name="SaveLogBtn" Click="SaveLog_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== Operations (control rows, no TweakCard chrome) ===== -->
            <TextBlock x:Name="OpsHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <AutoSuggestBox
                x:Name="OpsFilter"
                MaxWidth="420"
                HorizontalAlignment="Left"
                QueryIcon="Find"
                TextChanged="OpsFilter_TextChanged" />
            <!-- ONE persistent InfoBar for operation results. -->
            <InfoBar x:Name="OpsStatusBar" IsClosable="True" IsOpen="False" />
            <!-- Hand-built control rows with a subtle Fluent entrance animation. -->
            <StackPanel x:Name="OpsPanel" Spacing="0">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="24" IsStaggeringEnabled="True" />
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
