<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.FileLocksmithModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <controls:ModuleHeader x:Name="Header" Glyph="&#xE72E;" />
            <TextBlock x:Name="HeaderBlurb" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Path input + pickers -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0" VerticalAlignment="Center" />
            <Button x:Name="PickFileBtn" Grid.Column="1" Click="PickFile_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE8A5;" FontSize="14" />
                    <TextBlock x:Name="PickFileText" />
                </StackPanel>
            </Button>
            <Button x:Name="PickFolderBtn" Grid.Column="2" Click="PickFolder_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE8B7;" FontSize="14" />
                    <TextBlock x:Name="PickFolderText" />
                </StackPanel>
            </Button>
            <Button x:Name="ScanBtn" Grid.Column="3" Style="{StaticResource AccentButtonStyle}" Click="Scan_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE721;" FontSize="14" />
                    <TextBlock x:Name="ScanText" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Toolbar: status + refresh -->
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressRing x:Name="Busy" Grid.Column="0" Width="18" Height="18" IsActive="False" VerticalAlignment="Center" />
            <TextBlock x:Name="CountText" Grid.Column="1" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
            <Button x:Name="RefreshBtn" Grid.Column="2" Click="Refresh_Click" IsEnabled="False">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    <TextBlock x:Name="RefreshText" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Elevation hint -->
        <InfoBar x:Name="ElevationBar" Grid.Row="3" IsClosable="True" IsOpen="False" Severity="Warning">
            <InfoBar.ActionButton>
                <Button x:Name="ElevateBtn" Click="Elevate_Click" />
            </InfoBar.ActionButton>
        </InfoBar>

        <!-- Result feedback -->
        <InfoBar x:Name="ResultBar" Grid.Row="4" IsClosable="True" IsOpen="False" />

        <!-- Results list -->
        <Border
            Grid.Row="5"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Column headers -->
                <Grid Grid.Row="0" Padding="14,10,14,10" ColumnSpacing="10" Background="{ThemeResource LayerFillColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="78" />
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="70" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ColProc" Grid.Column="0" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColPid" Grid.Column="1" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColUser" Grid.Column="2" FontWeight="SemiBold" />
                    <TextBlock x:Name="ColFiles" Grid.Column="3" FontWeight="SemiBold" HorizontalAlignment="Right" />
                    <TextBlock x:Name="ColActions" Grid.Column="4" FontWeight="SemiBold" Width="240" />
                </Grid>

                <ListView x:Name="List" Grid.Row="1" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="6,8,6,8" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="78" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Process name + type + path -->
                                <StackPanel Grid.Column="0" Spacing="2">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding AppType}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                    <TextBlock Text="{Binding Path}" FontSize="11" FontFamily="Consolas" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding StartedText}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding Pid}" VerticalAlignment="Top" FontFamily="Consolas" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Grid.Column="2" Text="{Binding User}" VerticalAlignment="Top" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Grid.Column="3" Text="{Binding FileCount}" VerticalAlignment="Top" HorizontalAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="6" VerticalAlignment="Top">
                                    <Button Click="Files_Click" ToolTipService.ToolTip="Show locked files · 顯示鎖住嘅檔案" Padding="10,5,10,5">
                                        <FontIcon Glyph="&#xE8A5;" FontSize="12" />
                                    </Button>
                                    <Button Click="OpenLoc_Click" ToolTipService.ToolTip="Open file location · 開啟檔案位置" Padding="10,5,10,5">
                                        <FontIcon Glyph="&#xE838;" FontSize="12" />
                                    </Button>
                                    <Button Click="EndTask_Click" Padding="10,5,10,5">
                                        <StackPanel Orientation="Horizontal" Spacing="6">
                                            <FontIcon Glyph="&#xE711;" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                                            <TextBlock x:Name="EndTaskText" Text="End task · 結束" FontSize="12" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock x:Name="EmptyText" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextAlignment="Center" TextWrapping="Wrap" MaxWidth="420" Visibility="Collapsed" />
            </Grid>
        </Border>

        <!-- Footer note -->
        <TextBlock x:Name="FooterNote" Grid.Row="6" FontSize="11" TextWrapping="Wrap"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
    </Grid>
</Page>
