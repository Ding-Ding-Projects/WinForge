<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.IpInfoModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,16,24,24" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="820" Spacing="14">
            <controls:ModuleHeader x:Name="Header" Glyph="&#xE968;" />
            <TextBlock x:Name="HeaderBlurb" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button x:Name="RefreshBtn" Click="Refresh_Click" Style="{ThemeResource AccentButtonStyle}" />
            </StackPanel>

            <!-- Public IP -->
            <Border Padding="16,14,16,14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="1" VerticalAlignment="Center">
                        <TextBlock x:Name="PublicTitle" FontWeight="SemiBold" FontSize="15" />
                        <TextBlock x:Name="PublicIpText" FontSize="14" IsTextSelectionEnabled="True" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <Button x:Name="CopyBtn" Grid.Column="1" Click="Copy_Click" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Local adapters -->
            <TextBlock x:Name="AdaptersTitle" FontWeight="SemiBold" FontSize="15" />
            <TextBlock x:Name="AdaptersStatus" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <ListView x:Name="AdapterList" SelectionMode="None" IsItemClickEnabled="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4,0,4" Padding="16,14,16,14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
                                <TextBlock Text="{Binding Type}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{Binding Mac}" FontSize="13" IsTextSelectionEnabled="True" FontFamily="Consolas" />
                                <TextBlock Text="{Binding IPv4}" FontSize="13" IsTextSelectionEnabled="True" FontFamily="Consolas" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding IPv6}" FontSize="12" IsTextSelectionEnabled="True" FontFamily="Consolas" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{Binding Gateways}" FontSize="12" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Dns}" FontSize="12" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Speed}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
