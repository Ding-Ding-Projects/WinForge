<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <controls:ModuleHeader x:Name="Header" />
            <TextBlock
                x:Name="HeaderBlurb"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
        </StackPanel>

        <controls:SearchPatternBox
            x:Name="FilterBox"
            Grid.Row="1"
            MaxWidth="420"
            HorizontalAlignment="Left"
            AutomationName="Filter this section · 篩選呢個分類"
            PatternChanged="FilterBox_PatternChanged" />

        <ScrollViewer
            Grid.Row="2"
            Margin="0,4,0,0"
            Padding="0,0,8,16"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
                <ContentControl
                    x:Name="BrowserWorkbenchHost"
                    AutomationProperties.AutomationId="BrowserWorkbenchHost"
                    Visibility="Collapsed" />
                <ContentControl
                    x:Name="DeveloperWorkbenchHost"
                    AutomationProperties.AutomationId="DeveloperWorkbenchHost"
                    Visibility="Collapsed" />
                <StackPanel x:Name="CardsPanel" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
