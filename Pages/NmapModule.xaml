<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.NmapModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE968;" />
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <!-- Engine + consent bars -->
        <StackPanel Grid.Row="2" Spacing="6" Margin="0,2,0,0">
            <InfoBar x:Name="EngineBar" IsClosable="False" IsOpen="False" Severity="Warning" />
            <InfoBar x:Name="ConsentBar" IsClosable="True" IsOpen="False" Severity="Informational" />
            <InfoBar x:Name="ResultBar" IsClosable="True" IsOpen="False" />
        </StackPanel>

        <!-- Controls -->
        <Pivot Grid.Row="3" Margin="0,2,0,0">

            <!-- Scan tab -->
            <PivotItem x:Name="ScanTab">
                <StackPanel Spacing="10">

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="240" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock x:Name="TargetLabel" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBox x:Name="TargetBox" TextChanged="Any_Changed" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock x:Name="ProfileLabel" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <ComboBox x:Name="ProfileBox" HorizontalAlignment="Stretch" SelectionChanged="Any_Changed" />
                        </StackPanel>
                    </Grid>

                    <!-- Common flag toggles -->
                    <TextBlock x:Name="FlagsLabel" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <StackPanel x:Name="FlagsHost" Orientation="Horizontal" Spacing="14" />
                    <StackPanel x:Name="FlagsHost2" Orientation="Horizontal" Spacing="14" />

                    <!-- Extra flags -->
                    <StackPanel Spacing="4">
                        <TextBlock x:Name="ExtraLabel" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="ExtraBox" TextChanged="Any_Changed" />
                    </StackPanel>

                    <!-- Command preview -->
                    <TextBlock x:Name="PreviewLabel" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="10,8">
                        <TextBlock x:Name="PreviewText" FontFamily="Consolas" FontSize="12" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                    </Border>

                    <!-- Run row -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button x:Name="RunBtn" Style="{StaticResource AccentButtonStyle}" Click="Run_Click" />
                        <Button x:Name="CancelBtn" Click="Cancel_Click" IsEnabled="False" />
                        <Button x:Name="SaveBtn" Click="Save_Click" IsEnabled="False" />
                        <ProgressRing x:Name="Busy" IsActive="False" Width="22" Height="22" VerticalAlignment="Center" />
                        <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </StackPanel>
            </PivotItem>

            <!-- Operations tab -->
            <PivotItem x:Name="OpsTab">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="320">
                    <StackPanel x:Name="OpsPanel" Spacing="2" />
                </ScrollViewer>
            </PivotItem>

            <!-- Live log tab -->
            <PivotItem x:Name="LogTab">
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6" Padding="10">
                    <ScrollViewer x:Name="LogScroller" MaxHeight="320" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="LogText" FontFamily="Consolas" FontSize="12" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                    </ScrollViewer>
                </Border>
            </PivotItem>
        </Pivot>

        <!-- Results: hosts column header -->
        <Grid Grid.Row="4" Padding="8,0,8,0" ColumnSpacing="10" Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ColHost" Grid.Column="0" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColPort" Grid.Column="1" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColProto" Grid.Column="2" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColState" Grid.Column="3" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColService" Grid.Column="4" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColVersion" Grid.Column="5" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>

        <!-- Results grid -->
        <Border Grid.Row="5" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <ListView x:Name="List" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="8,4,8,4" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding HostDisplay}" VerticalAlignment="Center" FontFamily="Consolas" FontSize="12" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" Text="{Binding Port}" VerticalAlignment="Center" FontFamily="Consolas" FontSize="12" />
                                <TextBlock Grid.Column="2" Text="{Binding Protocol}" VerticalAlignment="Center" FontSize="12" />
                                <TextBlock Grid.Column="3" Text="{Binding State}" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Grid.Column="4" Text="{Binding Service}" VerticalAlignment="Center" FontSize="12" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="5" Text="{Binding Version}" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="EmptyHint" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                           TextAlignment="Center" MaxWidth="420" />
            </Grid>
        </Border>
    </Grid>
</Page>
