<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.ClipboardModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <controls:ModuleHeader x:Name="Header" Grid.Column="0" Glyph="&#xE77F;" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="QrBtn" Click="MakeQrFromClipboard_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xED14;" FontSize="14" />
                        <TextBlock x:Name="QrText" />
                    </StackPanel>
                </Button>
                <Button x:Name="ClearBtn" Click="Clear_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE74D;" FontSize="14" />
                        <TextBlock x:Name="ClearText" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <InfoBar x:Name="BgBar" Grid.Row="2" IsClosable="False" IsOpen="True" Severity="Success" Margin="0,2,0,0" />
        <InfoBar x:Name="ResultBar" Grid.Row="3" IsClosable="True" IsOpen="False" Margin="0,2,0,0" />

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Root" Spacing="8" />
        </ScrollViewer>
    </Grid>
</Page>
