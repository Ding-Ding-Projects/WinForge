<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.RegistryEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinForge.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource WinForgePageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ModuleHeader x:Name="Header" Grid.Row="0" Glyph="&#xE74C;" />

        <TextBlock
            x:Name="PathBar"
            Grid.Row="1"
            FontFamily="Consolas"
            IsTextSelectionEnabled="True"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <TreeView
                    x:Name="Tree"
                    Padding="4,8,4,8"
                    Expanding="Tree_Expanding"
                    ItemInvoked="Tree_ItemInvoked"
                    SelectionMode="Single" />
            </Border>

            <Grid Grid.Column="1" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                    <Button x:Name="NewBtn" Click="New_Click" />
                    <Button x:Name="EditBtn" Click="Edit_Click" IsEnabled="False" />
                    <Button x:Name="DeleteBtn" Click="Delete_Click" IsEnabled="False" />
                    <Button x:Name="RefreshBtn" Click="Refresh_Click" />
                </StackPanel>

                <Border
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <ListView
                        x:Name="ValuesList"
                        SelectionChanged="ValuesList_SelectionChanged"
                        SelectionMode="Single">
                        <ListView.HeaderTemplate>
                            <DataTemplate />
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="6,4,6,4" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding TypeText}" />
                                    <TextBlock Grid.Column="2" FontFamily="Consolas" Text="{Binding DataText}" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <InfoBar x:Name="ResultBar" Grid.Row="2" IsClosable="True" IsOpen="False" />
            </Grid>
        </Grid>
    </Grid>
</Page>
