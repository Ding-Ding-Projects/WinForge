<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.WindhawkModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,16,24,24" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="1000" Spacing="14">

            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon FontSize="24" Glyph="&#xE945;" VerticalAlignment="Center" />
                <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>
            <TextBlock
                x:Name="HeaderBlurb"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <!-- Install bar (shown only when Windhawk is missing) -->
            <InfoBar x:Name="EngineBar" IsClosable="False" IsOpen="False" Severity="Warning" />

            <!-- Status + primary actions -->
            <Border
                Padding="14"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                CornerRadius="8">
                <StackPanel Spacing="10">
                    <TextBlock
                        x:Name="StatusText"
                        FontWeight="SemiBold"
                        TextWrapping="Wrap" />
                    <StackPanel
                        x:Name="ActionRow"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Button x:Name="LaunchBtn" Click="Launch_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="ModsFolderBtn" Click="ModsFolder_Click" />
                        <Button x:Name="SiteBtn" Click="Site_Click" />
                    </StackPanel>
                    <InfoBar x:Name="ActionBar" IsClosable="True" IsOpen="False" />
                </StackPanel>
            </Border>

            <!-- About / how it works -->
            <Expander
                x:Name="AboutExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <TextBlock x:Name="AboutHeader" />
                </Expander.Header>
                <TextBlock
                    x:Name="AboutBody"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </Expander>

            <!-- Curated mod gallery -->
            <TextBlock x:Name="GalleryHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock
                x:Name="GalleryHint"
                FontSize="13"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
            <AutoSuggestBox
                x:Name="ModFilter"
                MaxWidth="420"
                HorizontalAlignment="Left"
                QueryIcon="Find"
                TextChanged="ModFilter_TextChanged" />
            <StackPanel x:Name="ModsPanel" Spacing="2" />
        </StackPanel>
    </ScrollViewer>
</Page>
