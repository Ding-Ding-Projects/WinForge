<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinForge.Pages.GlazeWmModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,16,24,24" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="1040" Spacing="14">

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon FontSize="24" Glyph="&#xE737;" VerticalAlignment="Center" />
                <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>
            <TextBlock
                x:Name="HeaderBlurb"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <!-- Engine (not installed) bar -->
            <InfoBar x:Name="EngineBar" IsClosable="False" IsOpen="False" Severity="Warning" />

            <!-- Aggressive-management warning -->
            <InfoBar
                x:Name="WarnBar"
                IsClosable="True"
                IsOpen="False"
                Severity="Informational" />

            <!-- Status + process control -->
            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="12">
                    <InfoBar x:Name="StatusBar" IsClosable="False" IsOpen="True" Severity="Informational" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="StartBtn" Click="Start_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="StopBtn" Click="Stop_Click" />
                        <Button x:Name="ReloadBtn" Click="Reload_Click" />
                        <Button x:Name="RefreshBtn" Click="Refresh_Click" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <CheckBox x:Name="StartupCheck" Click="Startup_Click" />
                        <TextBlock x:Name="VersionText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Structured config editor -->
            <TextBlock x:Name="ConfigHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock
                x:Name="ConfigPathText"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                IsTextSelectionEnabled="True"
                TextWrapping="Wrap" />

            <InfoBar x:Name="NoConfigBar" IsClosable="False" IsOpen="False" Severity="Warning" />

            <Border
                x:Name="StructuredBorder"
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="14">

                    <!-- Gaps -->
                    <TextBlock x:Name="GapsLabel" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <StackPanel Spacing="2">
                            <TextBlock x:Name="InnerGapLabel" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBox x:Name="InnerGapBox" Width="120" />
                        </StackPanel>
                        <StackPanel Spacing="2">
                            <TextBlock x:Name="OuterGapLabel" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBox x:Name="OuterGapBox" Width="120" />
                        </StackPanel>
                    </StackPanel>

                    <!-- General toggles -->
                    <ToggleSwitch x:Name="FocusHoverToggle" />
                    <StackPanel Spacing="2">
                        <TextBlock x:Name="StartupCmdLabel" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBox x:Name="StartupCmdBox" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="SaveStructuredBtn" Click="SaveStructured_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="ReloadStructuredBtn" Click="ReloadStructured_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Workspaces editor -->
            <TextBlock x:Name="WorkspacesHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="WorkspacesHint" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    <ItemsControl x:Name="WorkspaceList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,3" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="1" Click="RemoveWorkspace_Click" Tag="{Binding}">
                                        <FontIcon FontSize="14" Glyph="&#xE74D;" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="AddWorkspaceBtn" Click="AddWorkspace_Click" />
                        <Button x:Name="SaveWorkspacesBtn" Click="SaveWorkspaces_Click" Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Keybindings (read-only list) -->
            <TextBlock x:Name="KeybindHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel>
                    <Grid Padding="12,8" Background="{ThemeResource LayerFillColorDefaultBrush}" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="KeyColHeader" Grid.Column="0" FontWeight="SemiBold" FontSize="12" />
                        <TextBlock x:Name="CmdColHeader" Grid.Column="1" FontWeight="SemiBold" FontSize="12" />
                    </Grid>
                    <ListView x:Name="KeybindList" MaxHeight="280" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="0,4" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Keys}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                                    <TextBlock Grid.Column="1" Text="{Binding Commands}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <TextBlock x:Name="KeybindEmpty" Margin="12,8" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Visibility="Collapsed" />
                </StackPanel>
            </Border>

            <!-- Raw YAML editor -->
            <TextBlock x:Name="RawHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock
                x:Name="RawHint"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
            <TextBox
                x:Name="RawBox"
                MinHeight="240"
                MaxHeight="420"
                AcceptsReturn="True"
                FontFamily="Consolas"
                FontSize="13"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                TextWrapping="NoWrap" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="SaveRawBtn" Click="SaveRaw_Click" Style="{StaticResource AccentButtonStyle}" />
                <Button x:Name="ReloadRawBtn" Click="ReloadRaw_Click" />
                <Button x:Name="OpenFolderBtn" Click="OpenFolder_Click" />
                <Button x:Name="PickConfigBtn" Click="PickConfig_Click" />
                <Button x:Name="CreateConfigBtn" Click="CreateConfig_Click" />
            </StackPanel>

            <!-- CLI operations -->
            <TextBlock x:Name="OpsHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
            <AutoSuggestBox
                x:Name="OpsFilter"
                MaxWidth="420"
                HorizontalAlignment="Left"
                QueryIcon="Find"
                TextChanged="OpsFilter_TextChanged" />
            <StackPanel x:Name="OpsPanel" Spacing="2" />

        </StackPanel>
    </ScrollViewer>
</Page>
