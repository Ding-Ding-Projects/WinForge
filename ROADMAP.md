# WinForge Roadmap · 路線圖

## Native C++ migration · 原生 C++ 遷移

- [x] Integrate native Health Calculators locally: `bmi`, `health`, and `module.bmi` now use standard-C++ `Bmi` logic plus C++/WinRT UI in the controlled native shell. Debug/Release builds have 0 errors; both combined cores are **842/842** (BMI **14/14**), focused BMI UIA is **14/14**, catalog parity is **346 + 5**, and the installer contract passes. Accounting is **35/346** fixed routes (`35 in-progress / 311 not-started`). LowLevel MCP is not callable and the driver rejected its blank fallback, so visual remains `capture-blocked`; C++-only release policy is unchanged and remote push/release verification is next. · 完成本機受控整合原生健康計算器：三個 alias 已同原生 shell 用標準 C++ core 同 C++/WinRT UI。Debug／Release 0 errors、合併 core 各 **842/842**（BMI **14/14**）、BMI UIA **14/14**、catalog parity 346+5 同 installer contract 通過；固定 route 係 **35/346**（`35 in-progress / 311 not-started`）。LowLevel MCP 不可用、driver 拒絕空白 fallback，所以 visual 保持 `capture-blocked`；只限 C++ release policy 不變，下一步係遠端 push／release 驗證。

- [x] Integrate native Unit Price locally: `priceper`, `unitprice`, and `module.unitprice` now use standard-C++ `UnitPrice` logic plus C++/WinRT UI in the controlled native shell. Debug/Release builds have 0 errors; both combined cores are **828/828** (Unit Price **13/13**), focused UIA is **15/15**, Utility UIA is **39/39** including CSS Unit Converter, catalog parity is **346 + 5**, and the installer contract passes. Accounting is **34/346** fixed routes (`34 in-progress / 312 not-started`). The broad aggregate did not yield a final footer, so no full-shell pass is claimed. Visual remains `capture-blocked` because LowLevel MCP is not callable and the driver rejected its blank fallback; C++-only release policy is unchanged and remote push/release verification is next. · 完成本機受控整合原生 Unit Price：三個 alias 已同原生 shell 用標準 C++ core 同 C++/WinRT UI。Debug／Release 0 errors、合併 core 各 **828/828**（Unit Price **13/13**）、專項 UIA **15/15**、包括 CSS Unit Converter 嘅 Utility UIA **39/39**、catalog parity 346+5 同 installer contract 通過；固定 route 係 **34/346**（`34 in-progress / 312 not-started`）。廣泛 aggregate 冇最後 footer，唔聲稱 full-shell pass。LowLevel MCP 不可用、driver 拒絕空白 fallback，所以 visual 保持 `capture-blocked`；只限 C++ release policy 不變，下一步係遠端 push／release 驗證。
- [x] Integrate the native Slugify route: `slug`, `slugify`, and `module.slugify` now use standard-C++ `Slugify` logic plus a C++/WinRT renderer alongside native Reference Text and Morse. Combined evidence is Debug/Release 0-error builds, core **801/801**, catalog parity **346 + 5**, focused UIA **29/29** Reference Text, **13/13** Morse, **12/12** Slugify, and full shell **441/441**. The ledger is **32/346** fixed routes (`32 in-progress / 314 not-started`); visual evidence remains honestly `capture-blocked` because this session cannot call LowLevel MCP and the driver rejected its blank fallback. · 完成受控整合原生網址別名 route：三個 alias 用標準 C++ `Slugify` 同 C++/WinRT renderer，並同原生 Reference Text／Morse 共存。合併證據係 Debug／Release 0 errors、core **801/801**、catalog parity **346 + 5**、Reference Text **29/29**、Morse **13/13**、Slugify **12/12** 同完整 shell **441/441**；ledger 係 **32/346**（`32 in-progress / 314 not-started`）。今次冇可呼叫 LowLevel MCP，而 driver 拒絕空白 fallback，所以 visual 如實係 `capture-blocked`。
- [x] Integrate native Base Converter locally: `baseconvert` and `module.baseconvert` now use dependency-free standard-C++ arbitrary-precision conversion and C++/WinRT UI. Debug/Release builds have 0 errors; combined core suites are **857/857** (Base Converter **15/15**), focused UIA is **14/14**, catalog parity is **346 + 5** with 319 registry and 346 ledger rows, and the installer contract passes. Accounting is **36/346** fixed routes (`36 in-progress / 310 not-started`). Invalid-input diagnostics now match managed Unicode `Trim()`. Visual remains `capture-blocked` because no LowLevel MCP tool is callable and the fresh driver rejected its blank fallback; C++-only release policy is unchanged and remote push/release verification is next. · 完成本機受控整合原生進位轉換：`baseconvert` 同 `module.baseconvert` 而家用唔靠依賴嘅標準 C++ 任意精度轉換同 C++/WinRT UI。Debug／Release 0 errors、合併 core 各 **857/857**（Base Converter **15/15**）、專項 UIA **14/14**、catalog parity **346 + 5**（319 registry／346 ledger）同 installer contract 通過；固定 route 係 **36/346**（`36 in-progress / 310 not-started`）。無效輸入診斷而家同 managed Unicode `Trim()` 對等。LowLevel MCP 不可用、最新 driver 拒絕空白 fallback，所以 visual 保持 `capture-blocked`；只限 C++ release policy 不變，下一步係遠端 push／release 驗證。
- [ ] Continue porting every remaining fixed native route and five dynamic route families with route-specific core, UIA, documentation, and fresh visual evidence. A pending renderer is not a completed port. · 繼續逐項移植其餘固定 route 同五組動態家族，每項都要有 core、UIA、文件同最新視覺證據；pending renderer 唔算完成。
- [ ] Keep native CI/release publishing C++ artifacts only after tests pass, with exactly one uniquely tagged non-draft release for every push. · 保持原生 CI 只喺測試通過後發佈 C++ artifact，而且每次 push 只出一個唯一 tag、非草稿 release。
