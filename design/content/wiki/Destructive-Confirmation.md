# Destructive-action super confirmation · 破壞性動作超級確認

The shared confirmation surface makes destructive actions reviewable with two keys, a full slider, and a safe emergency exit. It is a local UX guard, not a security boundary. A dedicated dialog capture remains pending while the remaining destructive callers migrate. · 共用確認介面用兩條匙、完整滑桿同安全緊急離開，令破壞性動作可以先檢查；佢係本機 UX guard，唔係安全邊界。其餘破壞性 caller 遷移期間，專門 dialog 擷取仍然待做。
