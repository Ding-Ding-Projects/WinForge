# Native ASCII Table · 原生 ASCII 表

ascii, asciitable, and module.asciitable are now 37/346 fixed native routes. The local C++/WinRT page and standard-C++ core preserve default ASCII, explicit Latin-1, control-code distinctions, invariant search, virtualization, language retention, route reset, and explicit-only Copy. Debug/Release builds are 0-error; both cores pass 878/878 including 21/21 ASCII contracts; focused UIA is 16/16. Fresh visual capture is capture-blocked: no LowLevel tool is callable and the driver rejected the blank fallback.

ascii、asciitable 同 module.asciitable 而家計入 37/346 條固定原生 route。本機 C++/WinRT 頁同標準 C++ core 保留預設 ASCII、明確 Latin-1、控制碼分別、invariant 搜尋、虛擬化、語言保留、route reset 同只限明確 Copy。Debug／Release 0-error；兩個 core 各 878/878（ASCII 21/21）；專項 UIA 16/16。新 visual capture 係 capture-blocked：冇可呼叫 LowLevel 工具，而 driver 拒絕空白 fallback。
