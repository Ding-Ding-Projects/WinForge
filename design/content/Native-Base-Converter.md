# Native Base Converter · 原生進位轉換

**Controlled native integration / 受控原生整合：** `baseconvert` and `module.baseconvert` are now integrated into the native C++ working tree as a genuine local C++/WinRT page over a dependency-free standard-C++ arbitrary-precision core. It keeps base 2–36 signed conversion, grouped binary, 64-bit two's-complement display, BigInteger-compatible bitwise operations, state lifecycle, all language modes, and explicit-only Copy; release automation remains C++-only and unchanged. · `baseconvert` 同 `module.baseconvert` 而家已受控整合入原生 C++ working tree，係真正本機 C++/WinRT 頁加唔靠依賴嘅標準 C++ 任意精度 core。保留 2–36 有符號轉換、二進制分組、64-bit 二補數、BigInteger 相容 bitwise、狀態 lifecycle、全部語言模式同只限明確 Copy；release automation 仍然只限 C++、冇改動。

**Evidence / 證據：** Current Debug/Release x64 builds have 0 errors; both combined core suites are **857/857**, including **15/15** Base Converter contracts; focused UI Automation is **14/14** across both aliases; catalog parity is 346 fixed routes plus five dynamic families, 319 registry entries, and 346 ledger rows; and the native installer contract passes. Renderer accounting is **36/346** (`36 in-progress / 310 not-started`). The native diagnostic now shares the managed Unicode `Trim()` set, including non-breaking and ideographic whitespace. · 目前 Debug／Release x64 build 0 errors；合併 core 各 **857/857**（包括 Base Converter **15/15**）；兩個 alias 專項 UI Automation **14/14**；catalog parity 係 346 固定 route 加五組動態家族、319 個 registry entry 同 346 條 ledger row；native installer contract 通過。renderer 計數係 **36/346**（`36 in-progress / 310 not-started`）。原生診斷而家同 managed Unicode `Trim()` 字元集一致，包括不換行同全形空白。

**Visual status / 視覺狀態：** the repository-local LowLevel MCP checkout is present but no callable headless-desktop MCP tool is exposed in this Codex session. The fresh current `driver.ps1 -Native -Page baseconvert -WaitMs 16000` attempt reported `CopyFromScreen unavailable; captured the window through PrintWindow instead`, then rejected the blank/near-uniform client. No PNG was created or retained and no WinForge process remained, so this route is honestly `capture-blocked`, not visual-pass. · repository 本機 LowLevel MCP checkout 喺度，但今個 Codex session 冇可呼叫嘅 headless-desktop MCP 工具。最新目前 `driver.ps1 -Native -Page baseconvert -WaitMs 16000` 先報 `CopyFromScreen unavailable; captured the window through PrintWindow instead`，再拒絕空白／近乎單色 client。冇建立或保留 PNG、亦冇殘留 WinForge process，所以 route 如實係 `capture-blocked`，唔係 visual-pass。
