# Native ASCII Table · 原生 ASCII 表

The controlled native integration adds ascii, asciitable, and module.asciitable as a local C++/WinRT ASCII and explicit Latin-1 reference with virtualized rows, invariant search, language retention, route reset, and explicit-only raw-character Copy. Debug/Release builds are 0-error; both cores are 878/878 including 21/21 ASCII contracts; focused UIA is 16/16; catalog and installer checks pass. The local LowLevel MCP tool is unavailable and the fresh driver rejected a blank fallback, so visual evidence remains capture-blocked.

受控原生整合加入 ascii、asciitable 同 module.asciitable：本機 C++/WinRT ASCII／明確 Latin-1 參考表有虛擬化列、invariant 搜尋、語言保留、route reset 同只限明確 raw-character Copy。Debug／Release build 0-error；兩個 core 都係 878/878（ASCII 21/21）；專項 UIA 16/16；catalog 同 installer check 通過。今個 session 冇可呼叫 LowLevel MCP，最新 driver 又拒絕空白 fallback，所以 visual 仍然係 capture-blocked。

See the full Native ASCII Table verification record in ../Native-Ascii-Table.md.
