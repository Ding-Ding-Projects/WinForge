# Native C++ Rewrite · 原生 C++ 重寫

## Current 2026-07-19 Health Calculators controlled integration / 2026-07-19 最新健康計算器受控整合

`bmi`, `health`, and `module.bmi` now render a genuine C++/WinRT Health Calculators page over the standard-C++ `Bmi` core. The controlled combined checkpoint is **35/346 fixed routes** (**35 `in-progress` / 311 `not-started`**, plus five dynamic families): Debug and Release x64 solution builds have 0 errors, both core suites pass **842/842** including BMI **14/14**, focused BMI UI Automation passes **14/14**, catalog parity remains 346+5 with 319 registry records and 346 ledger rows, and the installer contract passes. The local LowLevel checkout is present but no callable MCP tool is exposed; the native driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, retained no PNG, and visual remains `capture-blocked`. No release workflow changed: publishing remains C++-only. · `bmi`、`health` 同 `module.bmi` 而家用標準 C++ `Bmi` core 開真正 C++/WinRT 健康計算器頁。受控合併檢查點係 **35/346** 條固定 route（**35 `in-progress` / 311 `not-started`**，另加五組動態家族）：Debug／Release solution 0 errors、core 各 **842/842**（BMI **14/14**）、BMI UIA **14/14**、catalog parity 346+5（319 registry／346 ledger）同 installer contract 都通過。LowLevel checkout 喺度但冇可呼叫 MCP 工具；driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，冇 PNG，visual 保持 `capture-blocked`。冇改 release workflow，發佈保持只限 C++。

## Current 2026-07-19 Unit Price controlled integration / 2026-07-19 最新單位價格受控整合

`priceper`, `unitprice`, and `module.unitprice` now render a genuine C++/WinRT Unit Price page over the standard-C++ `UnitPrice` core. The controlled combined checkpoint is **34/346 fixed routes** (**34 `in-progress` / 312 `not-started`**, plus five dynamic families): Debug and Release x64 solution builds have 0 errors, both core suites pass **828/828** including Unit Price **13/13**, focused Unit Price UI Automation passes **15/15**, Utility UI Automation passes **39/39** including CSS Unit Converter, catalog parity remains 346+5 with 319 registry records and 346 ledger rows, and the installer contract passes. A broad aggregate reached Unit Price but did not yield a captured final footer, so no completed full-shell result is claimed. The local LowLevel checkout is present but no callable MCP tool is exposed; the native driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, retained no PNG, and visual remains `capture-blocked`. No release workflow changed: publishing remains C++-only. · `priceper`、`unitprice` 同 `module.unitprice` 而家用標準 C++ `UnitPrice` core 開真正 C++/WinRT Unit Price 頁。受控合併檢查點係 **34/346** 條固定 route（**34 `in-progress` / 312 `not-started`**，另加五組動態家族）：Debug／Release solution 0 errors、core 各 **828/828**（Unit Price **13/13**）、Unit Price UIA **15/15**、包括 CSS Unit Converter 嘅 Utility UIA **39/39**、catalog parity 346+5（319 registry／346 ledger）同 installer contract 都通過。廣泛 aggregate 跑到 Unit Price，但冇擷取到最後 footer，所以唔聲稱完成 full shell。LowLevel checkout 喺度但冇可呼叫 MCP 工具；driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，冇 PNG，visual 保持 `capture-blocked`。冇改 release workflow，發佈保持只限 C++。

## Current 2026-07-19 Namespaced UUID controlled integration / 2026-07-19 最新具名空間 UUID 受控整合

`uuid5`, `uuidv5`, and `module.uuidv5` now render a genuine C++/WinRT Namespaced UUID page over the standard-C++ `UuidV5` core. It keeps deterministic RFC 4122 v3/MD5 and v5/SHA-1 network-byte-order generation, managed-compatible D/N/B/P/X namespace parsing, replacement UTF-16 handling, U+180E parity, local bulk rows, all three language modes, reset-on-reentry, and explicit-only clipboard writes. The controlled merge currently accounts for **33/346 fixed routes** (**33 `in-progress` / 313 `not-started`**, plus five dynamic families): fresh Debug and Release x64 native solution builds have 0 errors; both cores pass **815/815**; focused UUID UI Automation passes **21/21**; catalog parity is 346+5 with 319 registry records and 346 ledger rows; the native installer contract passes; and the aggregate native shell passes **469/469**. LowLevel MCP is not callable in this session; the fresh UUID driver found `CopyFromScreen` unavailable and rejected a blank/near-uniform `PrintWindow` frame, retaining no PNG. Visual evidence therefore remains honestly `capture-blocked`. No release workflow or managed-release path changed: publishing remains C++-only. · `uuid5`、`uuidv5` 同 `module.uuidv5` 而家會用標準 C++ `UuidV5` core 開真正 C++/WinRT 具名空間 UUID 頁。佢保留 RFC 4122 v3/MD5 同 v5/SHA-1 network-byte-order 穩定產生、managed 相容 D/N/B/P/X namespace parser、replacement UTF-16、U+180E parity、本機 bulk 行、三種語言、重新入 route 時重設，同埋只限明確 Copy 寫剪貼簿。今次受控 merge 計 **33/346** 條固定 route（**33 `in-progress` / 313 `not-started`**，另加五組動態家族）；最新 Debug／Release 原生 solution 各 0 errors、core 各 **815/815**、UUID 專項 UIA **21/21**、catalog parity 346+5（319 registry、346 ledger）、native installer contract 同完整 native shell **469/469** 都通過。今個 session 冇可呼叫 LowLevel MCP；最新 UUID driver 發現 `CopyFromScreen` 唔可用並拒絕空白／近乎單色 `PrintWindow` 畫面，冇保留 PNG。所以 visual 如實保持 `capture-blocked`。冇改 release workflow 或 managed release path，發佈保持只限 C++。
## Historical 2026-07-19 Unit Price feature-only handoff (pre-integration) / 歷史原生單位價格只限功能交接（整合前）

See [Native Unit Price](Native-Unit-Price.md) for the native `priceper`, `unitprice`, and `module.unitprice` implementation record. The isolated C++ feature branch has 0-error Debug/Release builds, **814/814** core in each configuration (Unit Price **13/13**), catalog parity **346 + 5**, and focused UIA **15/15**, bringing accounting to **33/346** (`33 in-progress / 313 not-started`). Its broad shell attempt passed every Unit Price assertion but stalled in an unrelated later CSS route and was stopped, so it is not a full-suite claim. LowLevel MCP is unavailable and the driver rejected a blank fallback; no PNG was promoted and visual evidence stays `capture-blocked`. · 原生 `priceper`、`unitprice` 同 `module.unitprice` 實作記錄見 [Native Unit Price](Native-Unit-Price.md)。獨立 C++ 功能分支有 Debug／Release 0 errors、core 各 **814/814**（Unit Price **13/13**）、catalog parity **346 + 5** 同專項 UIA **15/15**，計數係 **33/346**（`33 in-progress / 313 not-started`）。廣泛 shell 通過晒 Unit Price assertion 但之後喺不相關 CSS route 停住並已停止，唔係 full-suite 聲稱。LowLevel MCP 未可用而 driver 拒絕空白 fallback，冇升格 PNG，visual 保持 `capture-blocked`。

## Current 2026-07-19 Slugify controlled native integration / 2026-07-19 最新網址別名受控原生整合

Slugify (`slug`, `slugify`, `module.slugify`) is now integrated with the existing native Phonetic Speller, Box & Banner Text, HTML Entities, and Morse Code renderers. Its standard-C++ core preserves managed line splitting, diacritic/Unicode choices, separator/case/UTF-16 limits, preview, reset, localization, and explicit-only Copy. The combined checkpoint is **32/346 fixed routes** (**32 `in-progress` / 314 `not-started`**, plus five dynamic families): Debug and Release x64 builds have 0 errors, both cores pass **801/801**, catalog parity is 346+5, focused UIA is **29/29** Reference Text, **13/13** Morse, and **12/12** Slugify, and the exhaustive native shell is **441/441**. The local LowLevel checkout is present but no callable headless MCP tool is exposed in this session; the required native `slugify` driver attempt found `CopyFromScreen` unavailable and rejected a blank/near-uniform `PrintWindow` fallback. No PNG or canonical screenshot was changed, so the five routes remain honestly `capture-blocked`. · Slugify（`slug`、`slugify`、`module.slugify`）而家已同原生拼讀字母表、文字方框／橫幅、HTML 實體同 Morse Code renderer 受控整合。標準 C++ core 保留 managed 分行、重音／Unicode 選項、分隔符／大小寫／UTF-16 上限、預覽、重設、本地化同只限明確 Copy。合併檢查點係 **32/346** 條固定 route（**32 `in-progress` / 314 `not-started`**，另加五組動態家族）；Debug／Release 0 errors、core 各 **801/801**、catalog parity 346+5、Reference Text UIA **29/29**、Morse **13/13**、Slugify **12/12**，完整 shell **441/441**。本機 LowLevel checkout 喺度但今次 session 冇可呼叫 headless MCP 工具；必需 native `slugify` driver 發現 `CopyFromScreen` 不可用並拒絕空白／近乎單色 `PrintWindow` fallback。冇 PNG 或 canonical 截圖變更，所以五條 route 如實保持 `capture-blocked`。

**Hosted release proof / Hosted 發佈證明：** Slugify tip `bb853ef3` and controlled merge `88672704` are ancestors of `origin/main`. [Native run 29702620758](https://github.com/codingmachineedge/WinForge/actions/runs/29702620758) passed every native gate and published non-draft [`native-v1.0.70`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.70) exactly for `88672704`, with only `WinForge-Native-Setup.exe` and the versioned native ZIP. The successful [site-data run 29702620742](https://github.com/codingmachineedge/WinForge/actions/runs/29702620742) committed `ef822c1d`; its successful dispatched [native run 29702777841](https://github.com/codingmachineedge/WinForge/actions/runs/29702777841) promoted non-draft, non-prerelease [`native-v1.0.71`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.71) as Latest for that current main SHA, again with exactly those two native assets. · Slugify tip `bb853ef3` 同受控 merge `88672704` 都係 `origin/main` 祖先。[原生 run 29702620758](https://github.com/codingmachineedge/WinForge/actions/runs/29702620758) 通過所有原生 gate，準確為 `88672704` 發佈非 draft `native-v1.0.70`，只有 `WinForge-Native-Setup.exe` 同版本化原生 ZIP。成功嘅 [site-data run 29702620742](https://github.com/codingmachineedge/WinForge/actions/runs/29702620742) 提交 `ef822c1d`；佢 dispatch 嘅成功 [原生 run 29702777841](https://github.com/codingmachineedge/WinForge/actions/runs/29702777841) 將非 draft、非 prerelease `native-v1.0.71` 升做嗰個 current main SHA 嘅 Latest，仍然準確只有兩個原生 asset。

## Historical 2026-07-19 Reference Text + Morse Code integration / 2026-07-19 歷史參考文字及摩斯電碼整合

**Controlled integration evidence / 受控整合證據：** The combined native checkpoint is **31/346 fixed routes**, with **315** plus five dynamic route families remaining and **31 `in-progress` / 315 `not-started`** in the ledger. Debug and Release x64 solution builds have 0 errors; both core suites pass **783/783**; catalog parity covers 346 fixed routes plus five families; focused UI Automation passes **29/29** for Reference Text and **13/13** for Morse; and the exhaustive native shell passes **430/430**. LowLevel MCP is not callable here. The fresh native `morse` driver launch rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, leaving no PNG or canonical screenshot; all four routes remain `capture-blocked`. · 合併後係 **31/346** 條固定 route，仲有 **315** 條同五組動態家族，ledger 係 **31 `in-progress` / 315 `not-started`**；Debug／Release 0 errors、core 各 **783/783**、catalog parity、Reference Text UIA **29/29**、Morse UIA **13/13**、完整 shell **430/430** 全部通過。LowLevel MCP 喺呢度唔可呼叫；最新 `morse` driver 因 `CopyFromScreen` 不可用而拒絕空白 fallback，冇 PNG／冇 canonical 截圖，四條 route 保持 `capture-blocked`。

**Hosted release proof / Hosted 發佈證明：** [Native run 29700338773](https://github.com/codingmachineedge/WinForge/actions/runs/29700338773) passed all hosted native gates and published stable [`native-v1.0.64`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.64) at merge `dc95f0f8`. Its site-data follow-up committed `5e2636fd`; [native run 29700486762](https://github.com/codingmachineedge/WinForge/actions/runs/29700486762) then published current Latest [`native-v1.0.65`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.65) with exactly the native setup and portable ZIP. The downloaded v1.0.65 digests match GitHub; its 292-entry ZIP contains one MZ `WinForge.exe` and no managed WinForge payload. · Hosted run 29700338773 通過所有原生 gate，喺 merge `dc95f0f8` 發佈 stable native-v1.0.64；site-data 後 `5e2636fd` 嘅 run 29700486762 發佈 current Latest native-v1.0.65，準確只有原生 setup 同 portable ZIP。下載 v1.0.65 checksum 同 GitHub 一致；292-entry ZIP 只有一個 MZ `WinForge.exe`，冇 managed WinForge payload。
## Current 2026-07-19 Slugify checkpoint / 2026-07-19 最新網址別名檢查點

`slug`, `slugify`, and `module.slugify` now route to a dedicated native C++/WinRT Slugify page backed by standard-C++ logic. It keeps the managed line, normalization, accent, separator, case, Unicode opt-in, UTF-16 length, preview, reset, localization, and explicit-only clipboard contracts. Debug and Release builds exit 0 with 0 errors; both core suites are **777/777** with Slugify **18/18**; catalog parity covers 346 fixed routes plus five dynamic families; and focused UIA is **12/12** across all aliases including explicit Copy. LowLevel MCP is not callable here; the required driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, leaving no PNG, no worktree process, and no screenshot replacement. The ledger is honestly `in-progress` / `capture-blocked`. · `slug`、`slugify` 同 `module.slugify` 而家會開專用原生 C++/WinRT 網址別名頁，由標準 C++ 邏輯支援，保留 managed 分行、正規化、重音、分隔符、大小寫、Unicode 明確開啟、UTF-16 長度、預覽、重設、本地化同只限明確 Copy 合約。Debug／Release build 0 errors；兩個 core 各 **777/777**（Slugify **18/18**）；catalog parity 覆蓋 346 條固定 route 加五組家族；三個 alias UIA（包括明確 Copy）**12/12**。今次冇可呼叫 LowLevel MCP；`CopyFromScreen` 唔可用後 driver 拒絕空白 fallback，冇 PNG／冇 worktree process／冇替換截圖，所以 ledger 如實係 `in-progress`／`capture-blocked`。

## Current 2026-07-19 reference-text checkpoint / 2026-07-19 最新參考文字檢查點

Phonetic Speller, Box & Banner Text, and HTML Entities now have dedicated C++/WinRT renderers backed by standard-C++ `ReferenceText`. The routes preserve three phonetic alphabets, digit and option behavior, managed UTF-16 code-unit rows and spoken output; eight box/comment styles, alignment, bounded padding, titles, multiline/tab-aware layout and Unicode display width; and named/numeric HTML encoding and decoding, invalid UTF-16 safety, managed-compatible scanning and a local 50-row bilingual reference. All processing is local and clipboard writes require explicit Copy. · 拼讀字母表、文字方框／橫幅同 HTML 實體而家都有專用 C++/WinRT renderer，共用標準 C++ `ReferenceText`；保留三種拼讀表、數字／選項、同 managed 版一致嘅 UTF-16 code-unit 列、八種方框／註解風格、對齊／padding／標題、多行／tab-aware／Unicode 顯示闊度，以及具名／數字實體編碼解碼、無效 UTF-16 安全、managed 相容掃描同本機 50 項雙語參考。全部只喺本機做，只有明確 Copy 先會寫剪貼簿。

Renderer accounting is **30/346 fixed routes**, leaving **316 fixed routes** plus five dynamic families; the ledger is **30 `in-progress` / 316 `not-started`**. Fresh Debug and Release x64 solution builds each exit 0 with 0 errors, Debug and Release core each pass **759/759**, catalog parity covers 346 fixed routes plus five dynamic families, focused reference-text UIA is **29/29** across eight aliases, and the full native shell passes **417/417 (417 passed, 0 failed)**. LowLevel MCP is not callable in this session. A fresh post-integration `htmlentities` driver attempt found `CopyFromScreen` unavailable and rejected a blank/near-uniform `PrintWindow` fallback. No PNG was created or retained, no process remained, and no canonical screenshot changed, so visual evidence is `capture-blocked`. · Renderer 計 **30/346**，仲有 **316** 條固定 route 同五組動態家族；ledger 係 **30 `in-progress` / 316 `not-started`**。最新 Debug／Release 0 errors、兩個 core 各 **759/759**、catalog parity、八個 alias UIA **29/29** 同完整 native shell **417/417（417 passed、0 failed）** 已通過。今次 session 冇 LowLevel MCP；整合後最新 `htmlentities` driver 重試發現 `CopyFromScreen` 唔可用，並拒絕空白 fallback；冇建立或保留 PNG、冇殘留 process、冇改 canonical 截圖，所以 visual 係 `capture-blocked`。

## Historical isolated Morse Code branch checkpoint / 歷史獨立原生摩斯電碼分支檢查點

Morse Code (module.morse; aliases morse/module.morse) now has a dedicated C++/WinRT renderer and testable standard-C++ core. It matches managed encode/decode aliases, UTF-16 unknown-symbol handling, timing/WPM rules, explicit Copy, and lifecycle-safe timer cleanup on completion or route exit. The isolated branch records **28/346 fixed routes**, core Debug/Release **741/741** each including Morse **24/24**, focused UIA **13/13**, and catalog parity. LowLevel MCP is not callable in this Codex session; the driver launched the page but rejected a blank/near-uniform fallback, so no screenshot is promoted and visual evidence is honestly capture-blocked. See [Native Morse Code](Native-Morse-Code.md). · Morse Code 而家有專用 C++/WinRT renderer 同可測試標準 C++ core，保留 managed 編碼／解碼別名、UTF-16 未知符號處理、計時／WPM 規則、明確 Copy，同完成或離開 route 時安全清理 timer。獨立 branch 記錄 **28/346**、Debug／Release 各 **741/741**（Morse **24/24**）、專項 UIA **13/13** 同 catalog parity。呢個 Codex session 呼叫唔到 LowLevel MCP；driver 開到頁但拒絕空白／近乎單色 fallback，唔會提升 screenshot，visual 如實係 capture-blocked。

## Historical 2026-07-19 text-analysis collation checkpoint / 歷史 2026-07-19 文字分析排序檢查點

Text Statistics, Word Frequency, and String Compare now have dedicated C++/WinRT renderers backed by the standard-C++ `TextAnalysis` core. Text Statistics keeps local UTF-16/CJK counts, sentences, paragraphs, averages, reading/speaking times, Flesch readability, and top-ten words. Word Frequency keeps word/bigram/Unicode-scalar modes, options, current-culture ordering, count/bar/percentage rows, and exact CSV; String Compare keeps normalization, UTF-16 metrics, OSA/Jaro–Winkler/LCS metrics, the greater-than-2,000 guard, subquadratic exact greedy Jaro, and route-exit cleanup. Clipboard writes occur only after explicit Copy. · 文字統計、詞頻統計同字串相似度而家都有專用 C++/WinRT renderer，共用標準 C++ `TextAnalysis` core；保留 UTF-16／CJK 統計、句子／段落／平均值、閱讀／朗讀時間、Flesch／頭十詞，word／bigram／Unicode scalar 模式、目前文化排序、數量／條形／百分比同準確 CSV，以及正規化、UTF-16、OSA／Jaro–Winkler／LCS、超過 2,000 guard、subquadratic 準確 greedy Jaro 同 route-exit cleanup。只有明確 Copy 會寫剪貼簿。

Commit `c6f8a24d52e4949596efd58e070e0601a2939511` tightens native Word Frequency `CurrentCultureIgnoreCase` parity: the ICU comparer uses .NET Runtime width/kana tailoring, folds fullwidth case while retaining width/kana significance, keeps canonically equivalent terms stably ordered, and shares an initialized collator rather than serializing comparisons. The named total remains queryable without a per-edit polite live announcement; attribution is in `THIRD-PARTY-NOTICES.txt`. · Commit `c6f8a24d52e4949596efd58e070e0601a2939511` 對齊原生詞頻統計嘅 `CurrentCultureIgnoreCase`：ICU comparer 採用 .NET Runtime 闊度／假名 tailoring，fullwidth case 會折疊但闊度／假名仍有分別，canonical-equivalent 詞語保持穩定次序，而且共用已初始化 collator，唔會每次比較都序列化。命名 total 仍可查詢，但每次修改唔再發 polite live announcement；歸屬資料喺 `THIRD-PARTY-NOTICES.txt`。

Renderer accounting is **27/346 fixed routes**, leaving **319 fixed routes** plus five dynamic families; the ledger remains **27 `in-progress` / 319 `not-started`**. Debug/Release core is **717/717** each with Text Analysis **87/87**, focused UIA **40/40**, post-c6 exhaustive shell **388/388 (388 passed, 0 failed)**, and catalog parity for 346 fixed routes plus five families. The repo-local LowLevel checkout was present but not callable in this Codex session; the fresh `wordfreq` driver retry rejected a blank/near-uniform fallback and retained no PNG. Visual evidence is `capture-blocked`, not visual-pass. · Renderer 計 **27/346**，仲有 **319** 條固定 route 同五組動態家族；ledger 保持 **27 `in-progress` / 319 `not-started`**。Debug／Release core 各自 **717/717**，包括 Text Analysis **87/87**；專項 UIA **40/40**；c6 後完整 shell **388/388（388 passed、0 failed）**；catalog parity 覆蓋 346 條固定 route 加五組動態家族。repo 本機 LowLevel checkout 喺度但目前 Codex session 冇可呼叫工具；最新 `wordfreq` driver 重試拒絕空白／近乎單色 fallback，冇保留 PNG。Visual 係 `capture-blocked`，唔係 visual-pass。

## Earlier 2026-07-18 line-processing checkpoint / 2026-07-18 較早行文字處理檢查點

**Evidence-count note · 證據計數備註：** The durable managed-build gate is exit 0 with 0 errors; warning totals are not contractual. · Managed build 嘅長期 gate 係 exit 0 同 0 errors；warning 總數不屬合約。

Line Tools, Line Sort & Dedupe, and Text Wrap now have dedicated C++/WinRT renderer source backed by the shared standard-C++ `LineProcessing` core. At this earlier checkpoint, `HasNativeRenderer` and the parity ledger accounted for **24/346 fixed routes**, leaving **322 fixed routes** plus five dynamic route families. Native Debug and Release builds each exited 0; both core executables passed **630/630**, including **70/70** focused Line Processing contracts for UTF-16 line/count behavior, transforms, ordinal/natural sorting, deduplication, shuffle fixtures, and paragraph wrap/reflow. Catalog parity passed 346 fixed routes plus five dynamic families, and the managed build exited 0 with 0 errors. · 行工具、行排序同去重、文字換行而家有專用 C++/WinRT renderer source，共用標準 C++ `LineProcessing` core。喺呢個較早檢查點，`HasNativeRenderer` 同 parity ledger 計 **24/346 條固定 route**，仲有 **322 條固定 route** 同五組動態家族；Debug／Release build 各 exit 0，core 各 **630/630**（Line Processing **70/70**），catalog parity 通過，managed build exit 0、0 errors。

Focused `-LineRoutesOnly` UI Automation passes **42/42** across all seven registered aliases, and the full owned native shell smoke passes **348/348** with exit 0. LowLevel Computer Use MCP 1.28.1 launched `--page lines`, `--page textsort`, and `--page textwrap` on separate private desktops; the first pass resolved one **852×880** window per launch at PIDs 29740, 19176, and 25196, then cleaned every process and desktop. Its inspected `rendered_ok` `PrintWindow` frames were composition-white and invalid. The same persistent server's compositor retry started PIDs 21696, 23644, and 22048, but `show_headless_desktop` failed for all three pages with Win32 error 5, Access denied, before monitor capture; each PID was killed and desktop closed, including Text Sort's brief 0×0 ghost. The required run-winforge driver separately attempted each route with `-Native -WaitMs 16000`; `CopyFromScreen` was unavailable, every blank/near-uniform `PrintWindow` fallback was rejected, and each attempt exited 1. The artifact directory is empty, no invalid image remains, and no canonical screenshot was replaced. Launch/behavior evidence is green; visual evidence is conclusively `capture-blocked`. Hosted release-per-push and Git integration/remote proof remain pending. · `-LineRoutesOnly` 專項 UIA 喺全部七個 registered alias 通過 **42/42**，完整自有原生 shell smoke 亦以 exit 0 通過 **348/348**。LowLevel MCP 1.28.1 喺獨立 private desktop 準確開過三個 canonical command；第一輪每次都解析 **852×880** window，PID 係 29740／19176／25196，之後準確清理所有 process 同 desktop；檢查過嘅 `rendered_ok` `PrintWindow` 圖係 composition-white 無效畫面。同一 persistent server 嘅 compositor retry 開過 PID 21696／23644／22048，但三頁都因 Win32 error 5 Access denied 未能 `show_headless_desktop`；每個 PID 同 desktop 都已清理，包括 Text Sort 短暫嘅 0×0 ghost。必需 run-winforge driver 亦用 `-Native -WaitMs 16000` 試過三頁；`CopyFromScreen` 唔可用、空白／近乎單色 fallback 被拒，每次都 exit 1。Artifact directory 已清空，冇保留無效圖、冇替換 canonical 截圖；launch／behavior 已通過，visual 最終係 `capture-blocked`。Hosted release-per-push 同 Git 遙距證明仍待完成。

## Earlier 2026-07-18 utility checkpoint / 2026-07-18 較早工具檢查點

Text Diff, Aspect Ratio, and CSS Unit Converter are now dedicated genuine C++/WinRT routes with testable standard-C++ cores. `HasNativeRenderer` covers **21/346 fixed routes** and leaves **325 fixed routes** explicitly pending; the inventory also retains five dynamic route families. Debug and Release core each pass **560/560**; Design Tools passes **94/94**, focused utility UI Automation shell passes **39/39**, and the full owned shell remains **300/300**. Text Diff passes **27/27** focused contracts, including the exact 6,000,000-cell LCS guard and invariant Unicode casing. These local-only tools do not mutate Windows; explicit Copy is their only clipboard side effect. · 文字差異比對、長寬比計算同 CSS 單位換算而家係專用真正 C++/WinRT route，背後有可測試嘅標準 C++ core。`HasNativeRenderer` 涵蓋 **21/346 條固定 route**，另外 **325 條固定 route** 仍明確待完成；清單亦繼續涵蓋五組動態 route 家族。Debug 同 Release core 各自 **560/560**；Design Tools 通過 **94/94**，工具專項 UI Automation shell 通過 **39/39**，完整自有 shell 亦保持 **300/300**。文字差異比對有 **27/27** 個專項合約，包括準確 6,000,000-cell LCS 上限同 invariant Unicode 大小寫。呢三項本機工具唔會改 Windows，唯一剪貼簿副作用係明確 Copy。

LowLevel Computer Use MCP 1.28.1 and the repository driver separately launched all three pages, but every inspected client capture was uniformly white because WinUI composition is unavailable in this desktop session. Blank images were discarded and no canonical screenshot was replaced; see [Screenshots](Screenshots.md) for the exact bilingual evidence. All three routes are honestly `capture-blocked`. · LowLevel Computer Use MCP 1.28.1 同 repository driver 都分別開過三頁，但呢個 desktop session 冇 WinUI composition，所以每張檢查過嘅 client 擷取都係一致白色。空白圖已丟棄，冇替換 canonical 截圖；準確雙語證據見 [截圖集](Screenshots.md)。三條 route 如實係 `capture-blocked`。

## Earlier 2026-07-18 chmod checkpoint / 2026-07-18 早前 chmod 檢查點

The chmod Calculator is now a genuine C++/WinRT page backed by a standard-C++ `UnixPerm` core. `unixperm`, `chmod`, and `module.unixperm` preserve the managed `0644` default, all 12 permission bits, two-way octal/symbolic editing, `s/S/t/T`, atomic invalid-input retention, language state, and explicit-only clipboard Copy; the shown `chmod` command is never executed. Twenty focused contracts exhaust all 4,096 modes. Debug and Release core each pass **439/439** and the owned shell campaign passes **267/267**, including 14 live chmod checks. · chmod 計算機而家係真正 C++/WinRT 頁，由標準 C++ `UnixPerm` core 支援；三個 alias 完整保留 `0644`、12 個權限位、雙向輸入、`s/S/t/T`、錯誤保留、語言狀態同只限明確 Copy，亦絕不執行顯示嘅指令。20 個專項合約窮舉 4,096 個模式；Debug 同 Release 各 **439/439**，自有 shell **267/267**，包括 14 個即時 chmod 檢查。

`HasNativeRenderer` now records exactly 18 dedicated fixed-route renderers; All Apps labels only those as available and keeps other catalog routes pending. The corrected ledger is **18 in-progress + 328 not-started**, with About/Regex Cheatsheet evidence repaired and all nine Package Manager dynamic views recorded. Pending content is not parity. · `HasNativeRenderer` 準確登記 18 條有專用 renderer 嘅固定 route；All Apps 只將佢哋標示可用，其他保持待完成。修正 ledger 係 **18 in-progress + 328 not-started**，pending 內容唔算對等。

The required repo-local LowLevel MCP launched the exact native 1320×880 window from an immutable 294-file snapshot on a unique isolated desktop. Its inspected client was completely white (22,657 sampled pixels, one color, zero variance), and the already-High-integrity server could not `SwitchDesktop` because Windows returned error 5. The visible desktop was never used and no rejected PNG was promoted, so visual evidence remains `capture-blocked`. · 必需本機 LowLevel MCP 成功喺獨立隔離 desktop 開到準確 1320×880 原生視窗，但 client 全白，而 High integrity 下 `SwitchDesktop` 因錯誤 5 失敗。冇用可見 desktop，冇提升空白 PNG，所以視覺證據保持 `capture-blocked`。

Current native correction — 2026-07-17: Debug and Release core each pass **417/417**. The active bounded-PCRE2 set has six surfaces: Shell, All Apps, cached Package Discover, Regex Cheatsheet, Symbols Palette, and cached App Uninstaller. App Uninstaller is builder target **5**; Tester-only is **6**. Older 403/403, 226/226, and four-surface prose below is historical, not current headless-only evidence. LowLevel off-screen WinUI capture remains blocked: the client frame is blank and `NativePageTitle` does not appear after 30 seconds; no visible-desktop fallback is used.

目前原生更正 — 2026-07-17：Debug 與 Release core 各自 **417/417**；bounded-PCRE2 一共有六個 surface：Shell、All Apps、cached Package Discover、Regex Cheatsheet、Symbols Palette 同 cached App Uninstaller。App Uninstaller builder target 是 **5**，Tester-only 是 **6**。下面舊的 403/403、226/226 同四 surface 文字只屬歷史，不能當作 headless-only 目前證據。LowLevel off-screen WinUI client frame 仍然空白，30 秒後都冇 `NativePageTitle`；不會使用可見桌面回退。


## Native App Uninstaller · 原生應用程式解除安裝

## Current 2026-07-17 native checkpoint / 2026-07-17 最新原生檢查點

> This checkpoint supersedes older aggregate-count and deep-cleanup wording below. / 呢個檢查點取代下文較舊嘅總數同 deep-cleanup 文字。

- Native module.uninstall now has literal-default/bounded-PCRE2 cache filtering, invalid-result retention, review plus separate Confirm removal, and a normal-integrity fail-closed gate. Deep cleanup and every local-data deletion path are deliberately unavailable.
- The current RegexSearchSurface registry has six descriptors: Shell catalog, All Apps, cached Package Discover, Regex Cheatsheet, Symbols Palette, and cached App Uninstaller. Regex Builder index 5 returns to App Uninstaller.
- Debug/Release core evidence is **417/417** each; the LowLevel headless App Uninstaller UI Automation path is currently blocked because the off-screen WinUI client is blank and `NativePageTitle` is absent after 30 seconds. It does not fall back to a visible desktop. The broad post-hardening shell smoke timed out in unrelated checks and is not claimed as current full-suite evidence.
- Fresh App Uninstaller visual capture is capture-blocked after the required 2026-07-17 driver retry; stale managed images are retired.

- 原生 module.uninstall 有 literal 預設／bounded-PCRE2 快取篩選、無效結果保留、覆核加獨立 Confirm 移除，同正常 integrity fail-closed gate。deep cleanup 同所有本機資料刪除路徑刻意未開放。
- 最新 RegexSearchSurface registry 有六個 descriptor：Shell catalog、All Apps、cached Package Discover、Regex Cheatsheet、Symbols Palette、同 cached App Uninstaller。Regex Builder index 5 會返回 App Uninstaller。
- Debug/Release core 證據係各 **417/417**；最新 focused LowLevel App Uninstaller UI Automation smoke 通過五個安全／篩選／builder check，冇真實套件 mutation。廣泛 post-hardening shell smoke 喺無關 check timeout，唔聲稱係最新完整 suite 證據。
- required 2026-07-17 driver retry 後，App Uninstaller 新 visual capture 係 capture-blocked；舊 managed 圖已退休。

> Historical pre-hardening summary, superseded by the checkpoint above: Native module.uninstall is a real C++/WinRT current-user Store/UWP cache with local literal-default and opt-in bounded-PCRE2 filtering, invalid-result retention, review plus explicit Confirm removal, and the earlier optional-cleanup design. The current design deliberately exposes no deep cleanup. See [Native App Uninstaller](Native-App-Uninstaller.md).

> 歷史 hardening 前摘要（由上面 2026-07-17 檢查點取代）：早期 optional-cleanup／248-of-248 記錄只保留作背景；目前設計刻意冇 deep cleanup、冇本機資料刪除，而最新 UI 證據係 focused smoke。
## Native installer CI · 原生安裝程式 CI

The native installer contract is checked before packaging, after setup compilation, and after silent installation. It enforces per-user x64 policy, PE files, notices, no packaged debug symbols, exact setup output, and uninstall cleanup. See [Native Installer CI](Native-Installer-CI.md).

原生 installer contract 會喺封裝前、setup 編譯後同靜默安裝後驗證；佢強制每用戶 x64 policy、PE files、notices、冇 packaged debug symbols、setup output 正確，同埋解除安裝清理完成。詳情見 [Native Installer CI](Native-Installer-CI.md)。


## Native Symbols Palette · 原生特殊符號調色盤

**EN.** The Symbols Palette is now a real C++/WinRT route backed by a pure-C++ catalog of 226 local glyphs across nine bilingual categories. It keeps literal-default and opt-in bounded-PCRE2 filtering, explicit clipboard Copy, category fallback, and Regex Builder return behavior local to the native app. Debug and Release core tests passed 411/411; the owned LowLevel MCP native shell campaign passed 238/238.

**粵語.** 特殊符號調色盤而家係真正 C++/WinRT route，用純 C++ 226 個本機字形、九個雙語分類。佢保留文字預設、可選 bounded-PCRE2 過濾、明確複製、分類 fallback 同 Regex Builder 返回行為，全部留喺原生 app。Debug 同 Release core tests 都係 411/411；LowLevel MCP 原生 shell campaign 238/238 通過。

**Visual evidence / 視覺證據.** The owned capture driver could not use CopyFromScreen and rejected a blank or near-uniform PrintWindow fallback. No screenshot was retained or replaced; the route remains honestly capture-blocked until a graphics-capable isolated desktop is available. See Native-Symbols-Palette.md and wiki/Symbols-Capture-Status.md.


WinForge is being migrated to a genuine C++20/C++/WinRT WinUI 3 application. C# and XAML remain only as behavioral-oracle source while the native route ledger is completed; the native executable does not host the CLR, wrap the managed app, use C++/CLI, or outsource behavior over IPC. · WinForge 正喺遷移去真正嘅 C++20/C++/WinRT WinUI 3 app。C# 同 XAML 喺完成原生 route 清單前只係行為基準來源；原生 executable 唔會 host CLR、包住受控 app、用 C++/CLI，或者用 IPC 外判行為。

## Inventory · 清單

| Surface · 介面 | Count · 數量 |
|---|---:|
| Registry entries · Registry 記錄 | 319 |
| Fixed routes · 固定路線 | 346 |
| Dynamic route families · 動態路線組 | 5 |
| Runtime categories · 執行時分類 | 22 |

Unported routes render an explicit native pending page; routing to a page is not claimed as feature parity. · 未移植 route 會顯示明確原生 pending 頁；開到頁面唔代表功能對等。

## Current evidence · 目前證據

- Debug native build: 0 errors; Release native build: 0 errors. · Debug 原生建置：0 errors；Release 原生建置：0 errors。
- Native core tests: **403/403** in Debug and **403/403** in Release, including **14/14** focused Password Generator, **11/11** focused Password Strength, **6/6** focused Regex Cheatsheet, and **8/8** Regex Tester all-match/replacement checks. · 原生核心測試：Debug **403/403**、Release **403/403**，包括 **14/14** 個 Password Generator、**11/11** 個 Password Strength、**6/6** 個 Regex Cheatsheet 同 **8/8** 個 Regex Tester all-match／replacement 專項檢查。
- Catalog parity: 346 fixed routes, five dynamic families, 319 registry records, 22 categories, and 346 ledger rows. · 目錄對等：346 條固定路線、五組動態家族、319 條 registry 記錄、22 個分類同 346 條 ledger rows。
- Elevated process-owned UI Automation smoke: **226/226**, covering routing, bilingual accessibility, live controls, native regex search/builder (including x/n flags, all-match metadata, replacement preview, and output cap), Regex Cheatsheet catalog/filter/Copy/builder handoff, UUID v7, Password Generator, Password Strength masking/reveal/local warnings/language retention, Package Manager batch-consent policy, and clipping checks. · 提權、自有 process UI Automation smoke：**226/226**，涵蓋導覽、雙語無障礙、即時控制、原生 regex 搜尋／建立器（包括 x/n 旗標、all-match metadata、replacement preview 同 output cap）、Regex Cheatsheet 目錄／篩選／Copy／builder handoff、UUID v7、Password Generator、Password Strength 遮蔽／顯示／本機警告／語言保留、Package Manager 批次確認政策同裁切檢查。

This is native core/UI and fail-closed evidence, not normal-integrity external package-manager execution proof. · 呢啲係原生 core／UI 同 fail-closed 證據，唔係正常 integrity 外部 Package Manager 執行證明。

## Package Manager native slice · Package Manager 原生批次

The native Package Manager is in progress, not a full UniGetUI clone. It has nine views, eleven Windows manager adapters, read-only Discover/Updates/Installed queries, Details/Sources probes, local update rules, native preferences, and a bounded metadata-only v3 Bundle workspace. · 原生 Package Manager 仍然進行中，唔係完整 UniGetUI 複製。佢有九個 view、十一個 Windows manager adapter、只讀 Discover／Updates／Installed 查詢、Details／Sources 探測、本機更新規則、原生偏好，同有界只存 metadata 嘅 v3 Bundle 工作區。

One cached Discover, Updates, or Installed row may create a redacted reviewed Install/Update/Uninstall argv plan in `AwaitingConsent`; a separate Confirm action queues it for serial normal-integrity execution. Source-aware selected cached rows and the current eligible Review Update all set can now instead submit one all-or-nothing batch of at most 25 fully visible redacted argv plans. Its batch card has one explicit confirmation, serial ordered execution, combined cancellation, and retry that returns only unsuccessful children to fresh consent without replaying successful commands. The 50-record in-memory coordinator rejects elevation, hooks, unsafe IDs, custom mutation arguments, and an oversized consent preview; it retains safe reviewed/lifecycle metadata and withholds third-party stdout, stderr, and runtime diagnostics. · 一條已快取 Discover、Updates 或 Installed 資料列可以喺 `AwaitingConsent` 建立已遮蔽已檢視安裝／更新／解除安裝 argv 計劃；要由另一個 Confirm 動作先會排入串行正常 integrity 執行。識別來源嘅已揀快取資料列同目前合資格嘅檢視全部更新集合而家亦可以提交一個全有或全無、最多 25 條完整可見已遮蔽 argv 計劃嘅批次。批次卡有一次明確確認、按次序串行執行、一齊取消，同只會將未成功子項返回全新確認而唔會重播成功指令。50 條記憶體協調器會拒絕提升權限、hooks、唔安全 ID、自訂修改參數同過長確認預覽；佢會保留安全已檢視／生命週期 metadata，並略去第三方 stdout、stderr 同執行時診斷。

Normal-integrity external query/mutation proof is blocked in this elevated session, and the runtime fails closed. Elevation mediation, scheduler/notification integration, full bundle interoperability, Setup/bootstrap, richer manager-specific UX, and broader settings remain pending. · 正常 integrity 外部查詢／修改證明喺呢個提權 session 受阻，而 runtime 會 fail closed。提升權限調停、排程／通知整合、完整 Bundle 互通、Setup／bootstrap、豐富逐管理器 UX 同更廣設定仍待完成。

## Native regex search and builder · 原生正規搜尋同建立器

`RegexSearchSurface.h` registers all four current native regex-search controls—Shell catalog, All Apps, cached Package Discover, and the static local Regex Cheatsheet. Each defaults to literal filtering and enters bounded PCRE2-16 only through its explicit Regex mode (UTF/UCP, no JIT, pattern/input/nesting/code-size/match/depth/heap limits, and a 10 ms callout). `module.regextester` is a full four-step native builder for flags; escaped literal, route, package, and semantic-version recipes; safe tokens; grouping/alternation/word-boundary/lookaround assertions/quantifiers; and match/capture preview before applying only a valid pattern to a selected native target. It is not a claim of full .NET regex/replacement parity. · `RegexSearchSurface.h` 登記咗而家全部四個原生 regex-search 控制——Shell 目錄、所有 app、已快取 Package Discover 同靜態本機 Regex Cheatsheet。每個預設 literal 篩選，只有經明確 Regex mode 先進入有界 PCRE2-16（UTF/UCP、唔用 JIT、pattern／input／nesting／code-size／match／depth／heap 限制同 10 ms callout）。`module.regextester` 係完整四步原生建立器，處理旗標、已 escape literal／路線／套件／語義版本 recipe、安全 token、grouping／alternation／word-boundary／lookaround assertion／quantifier，同只會套用有效模式去已揀原生目標之前嘅 match／capture 預覽。呢個唔係完整 .NET regex／replacement 對等聲稱。

The native tester continuation enumerates up to **100** non-overlapping matches, reports named capture metadata, and shares its bounded deadline across the enumeration while safely advancing zero-length matches. The builder exposes PCRE2 extended whitespace `(x)` and named-capture-only `(n)`, persists those states to all four native search targets, and provides a local replacement preview restricted to `$$`, existing `$0`–`$99`, and `${name}`. Invalid replacement text or a 32 KiB output cap clears the preview and cannot affect a target. Debug and Release each pass **403/403**; isolated LowLevel MCP headless UI Automation passes **226/226**. The inspected 852×880 full-window and 836×841 client-only captures were blank and discarded, so this page remains `capture-blocked`, not a visual-pass claim. · 原生 Tester 延續而家會列舉最多 **100** 個非重疊相符、報告命名 capture metadata，並且喺列舉期間共用受限制 deadline，同時安全處理零長度相符。Builder 提供 PCRE2 `(x)` 忽略 pattern 空白同 `(n)` 只保留命名 capture，將狀態保存去四個原生搜尋 target，並提供只限 `$$`、存在嘅 `$0`–`$99` 同 `${name}` 嘅本機 replacement preview。無效 replacement 或 32 KiB output cap 會清空預覽，唔可以影響 target。Debug 同 Release 都係 **403/403**；isolated LowLevel MCP headless UI Automation 係 **226/226**。852×880 full-window 同 836×841 client-only 截圖係空白並已丟棄，所以呢頁仍然係 `capture-blocked`，唔係 visual-pass 聲稱。

## Native Regex Cheatsheet · 原生 Regex 速查表

`module.regexcheat` now opens a real C++/WinRT page backed by pure C++: 67 bilingual .NET-regex reference rows in nine categories and eight explicit Copy-only ready-made patterns. .NET-only constructs are inert documentation, not executable behavior; the route cannot launch a process, make a network request, or change a package argv. Literal filtering is default, opt-in bounded PCRE2 preserves the prior visible entries for invalid input, and the full builder returns a verified expression to this fourth native search target. Debug/Release pass **395/395** (six focused checks), catalog parity covers 346 fixed routes plus five dynamic families, and isolated LowLevel MCP UI Automation passes **224/224** including filtering, invalid retention, Copy feedback, builder handoff, accessibility, and clipping. · `module.regexcheat` 而家開真正嘅 C++/WinRT 頁面，背後係純 C++：67 項雙語 .NET-regex 參考、九個分類，同八個明確 Copy-only 現成模式。.NET 專用構造係惰性文件，唔係可執行行為；route 唔可以啟動 process、發 network request 或改 package argv。literal 篩選係預設，opt-in 有界 PCRE2 喺無效輸入時保留之前可見項目，而完整 builder 會將已驗證表達式交返呢個第四個原生搜尋 target。Debug／Release 係 **395/395**（六個專項檢查），catalog parity 涵蓋 346 條固定 routes 同五組 dynamic families，而隔離 LowLevel MCP UI Automation 係 **224/224**，覆蓋篩選、無效保留、Copy feedback、builder handoff、accessibility 同 clipping。

Discover regex is local cache only: it never reaches argv or HTTPS, external Search is disabled while it is active, and `NativePackageQueryAudit` changes only for a genuine package query. Applying the builder validates first, forces the Discover view, and retains only a completed Discover cache—never stale Update/Installed rows. PCRE2 attribution ships in [`THIRD-PARTY-NOTICES.txt`](../../THIRD-PARTY-NOTICES.txt), the portable ZIP, and the native installer. · Discover regex 只限本機快取：絕對唔會傳去 argv 或 HTTPS，啟用時會停用外部 Search，而且 `NativePackageQueryAudit` 只會喺真正套件查詢時改。套用 builder 會先驗證、強制 Discover view，而且只保留已完成嘅 Discover 快取——絕對唔會保留舊 Update／Installed 資料列。PCRE2 歸屬會放喺 [`THIRD-PARTY-NOTICES.txt`](../../THIRD-PARTY-NOTICES.txt)、可攜 ZIP 同原生 installer。

## Native UUID v7 · 原生 UUID v7

`module.uuidv7` is now a native C++ RFC 9562 utility reachable through `uuidv7` and the canonical route. It creates 1–1000 canonical v7 values from the 48-bit Unix-millisecond field plus cryptographic randomness; its optional monotonic mode stays ordered through same-millisecond generation, backwards clocks, and `rand_a` overflow. Local decoding accepts canonical, compact, braced, and `urn:uuid:` forms, reports version/variant plus UTC/local time for v7 and best-effort v1, canonicalizes lowercase output, and collapses stale result controls after invalid input. Clipboard writes require explicit Copy, while state survives language rerender. The row is `in-progress` solely because visual capture is blocked. · `module.uuidv7` 而家係原生 C++ RFC 9562 utility，可以用 `uuidv7` 同本體 route 開啟。佢由 48-bit Unix millisecond 欄位加密碼學隨機值產生 1–1000 條標準 v7 值；可選 monotonic 模式會喺同一毫秒產生、時鐘倒退同 `rand_a` overflow 時維持次序。本機解碼接受標準、緊湊、花括號同 `urn:uuid:` 格式，會報告 version／variant 加 v7 同 best-effort v1 嘅 UTC／本機時間、標準化小楷輸出，並喺無效輸入後摺疊舊結果控制。剪貼簿寫入要明確 Copy，而狀態會喺轉語言後保留。清單列只係因視覺擷取受阻先保持 `in-progress`。

## Native Password Generator · 原生密碼產生器

`module.passgen` is a live native C++ Password Generator reachable through `passgen`, `password`, and the canonical route. Its testable core uses Windows BCrypt cryptographic randomness with unbiased rejection sampling; it produces 4–128-character passwords with guaranteed selected classes, optional ambiguous-character removal and no-repeat enforcement, plus 3–10-word passphrases from the current 252-word dictionary with an editable separator, capitalization, and an optional trailing digit. Entropy is shown in both modes, output supports 1–100 rows, language rerenders retain generated content, and the clipboard changes only through explicit Copy. The row remains `in-progress` solely because fresh visual capture is blocked. · `module.passgen` 係即時原生 C++ 密碼產生器，可以用 `passgen`、`password` 同本體 route 開啟。可測試 core 用 Windows BCrypt 加密碼學隨機值配合無偏 rejection sampling；會產生 4–128 字元、保證已揀類別嘅密碼，可選移除易混淆字元同禁止重複，亦可用現有 252 字詞字典產生 3–10 字通行短語，支援可編輯分隔符、大寫同可選尾數字。兩種模式都有熵值、輸出支援 1–100 行、轉語言時保留已產生內容，而且剪貼簿只會經明確 Copy 改動。清單列只因最新視覺擷取受阻而保持 `in-progress`。

## Native Password Strength · 原生密碼強度

`module.passwordstrength` is a native C++ local-only Password Strength analyzer available through `passwordstrength`, `pwstrength`, and the canonical route. It calculates ASCII-pool entropy, rating and crack-time bands, and local common-password, repeat, and sequence warnings. The editor is masked by default; reveal is explicit and in-memory only, the value clears on navigation, and there is no persistence, log, network, or clipboard path. The source guards delayed programmatic events from an inactive editor so hide/show never clears the analysis model. The focused core suite has 11 checks, and the 224/224 headless UI smoke covers masking, reveal, common-password warnings, aliases, language retention, accessibility, and horizontal bounds. The row remains `in-progress` solely because the inspected LowLevel 852×880 capture had a blank client surface and was discarded. · `module.passwordstrength` 係原生 C++、只限本機嘅密碼強度分析器，可以用 `passwordstrength`、`pwstrength` 同本體 route 開啟。佢會計算 ASCII 字元池熵值、評級同破解時間範圍，以及本機常見密碼、重複同序列警告。輸入預設遮蔽；顯示係明確而且只喺記憶體，導覽時會清除，而且冇持久化、記錄、網絡或者剪貼簿路徑。原始碼會保護非活動輸入框嘅延遲程式事件，所以隱藏／顯示唔會清除分析模型。專項 core 有 11 個檢查，而 224/224 無頭 UI smoke 覆蓋遮蔽、顯示、常見密碼警告、alias、語言保留、無障礙同水平邊界。呢項只因檢查過嘅 LowLevel 852×880 擷取有空白 client surface、已經丟棄而保持 `in-progress`。

## Visual and accessibility evidence · 視覺同無障礙證據

On 2026-07-16, Regex Cheatsheet was launched on an isolated LowLevel MCP headless desktop after the **224/224** shell smoke. The resolved 852×880 full-window frame showed a title bar and blank client surface; the inspected 836×841 client-only frame was also blank. Both invalid PNGs were discarded. The route is `capture-blocked`, not visual-pass; the 395/395 Debug/Release test gate and 224/224 UI Automation result are behavioral/accessibility evidence only. · 2026-07-16，Regex Cheatsheet 喺 **224/224** shell smoke 後於隔離 LowLevel MCP 無頭 desktop 開啟。已解析嘅 852×880 full-window frame 有 title bar 同空白 client surface；檢查過嘅 836×841 client-only frame 亦係空白。兩個無效 PNG 都已丟棄。route 係 `capture-blocked`，唔係 visual-pass；395/395 Debug／Release 測試閘門同 224/224 UI Automation 結果只係行為／無障礙證據。

On 2026-07-16, the repository driver launched the changed Dashboard, All Apps, Regex Tester, and Package Discover pages with `-WaitMs 16000`; `CopyFromScreen` was unavailable and each `PrintWindow` fallback was rejected as blank or near-uniform. The repo-local LowLevel MCP isolated desktop separately launched Regex Tester, resolved its 1980×1320 WinUI HWND, and produced an inspected full-window frame with only a title bar and blank client surface. The invalid PNG was discarded. No invalid, stale, synthetic, or managed screenshot is presented as native evidence; changed native pages are `capture-blocked`. The 171/171 UI Automation result remains behavioral/accessibility evidence only. · 2026-07-16，repository driver 用 `-WaitMs 16000` 開啟改過嘅 Dashboard、所有 app、Regex Tester 同 Package Discover 頁；`CopyFromScreen` 用唔到，而且每個 `PrintWindow` fallback 都因空白／接近單色而拒絕。repo 本機 LowLevel MCP 隔離 desktop 亦獨立開啟 Regex Tester、解析佢 1980×1320 嘅 WinUI HWND，並產生一張檢查過嘅完整視窗 frame，只得 title bar 同空白 client surface；無效 PNG 已丟棄。冇將無效、舊、合成或者受控截圖當做原生證據；改過原生頁面係 `capture-blocked`。171/171 UI Automation 結果只係行為／無障礙證據。

On 2026-07-16, the required driver also launched `uuidv7` at `-WaitMs 16000` but rejected its blank/near-uniform `PrintWindow` fallback after `CopyFromScreen` was unavailable. The requested LowLevel MCP isolated desktop then launched UUID v7, resolved its 1980×1320 WinUI HWND, and produced inspected captures: the full window had only its title bar and blank client surface, and the 1958×1264 client-only frame was blank. Both invalid PNGs were discarded; no canonical, wiki-local, stale, synthetic, or managed image was replaced or reused. UUID v7 is `capture-blocked`, and that earlier **185/185** UI Automation checkpoint is behavioral/accessibility evidence only. · 2026-07-16，必需 driver 亦用 `-WaitMs 16000` 開啟 `uuidv7`，但喺 `CopyFromScreen` 用唔到後拒絕咗空白／接近單色 `PrintWindow` fallback。按要求嘅 LowLevel MCP 隔離 desktop 跟住開啟 UUID v7、解析佢 1980×1320 WinUI HWND，並產生檢查過嘅擷取：完整視窗只得 title bar 同空白 client surface，而 1958×1264 只限 client frame 亦係空白。兩張無效 PNG 已丟棄；冇替換或者重用 canonical、wiki 本機、舊、合成或者受控圖片。UUID v7 係 `capture-blocked`，而嗰個較早 **185/185** UI Automation 檢查點只係行為／無障礙證據。

The preceding 171/171 and 185/185 assertions are earlier checkpoints; the current aggregate is shown above. · 上面 171/171 同 185/185 斷言係較早檢查點；目前整體結果喺上面。

On 2026-07-16, the required driver launched `passgen` at `-WaitMs 16000`, but `CopyFromScreen` was unavailable and the `PrintWindow` fallback was blank or near-uniform, so no image was accepted. The requested LowLevel MCP tools are not registered in the active Codex session, so no LowLevel capture is claimed. No canonical, wiki-local, stale, synthetic, or managed image was replaced or reused. Password Generator is `capture-blocked`; **200/200** UI Automation is behavioral/accessibility proof only. · 2026-07-16，必需 driver 用 `-WaitMs 16000` 開啟 `passgen`，但 `CopyFromScreen` 用唔到，而 `PrintWindow` fallback 係空白／接近單色，所以冇接受圖片。要求嘅 LowLevel MCP tools 未有登記喺目前 Codex session，所以唔會聲稱有 LowLevel 擷取。冇替換或者重用 canonical、wiki 本機、舊、合成或者受控圖片。Password Generator 係 `capture-blocked`；**200/200** UI Automation 只係行為／無障礙證明。

## Completion gate · 完成閘門

Cutover requires every route, control, service, companion, launcher, updater, installer, protocol, test, documentation mirror, and changed-page screenshot to pass, with a final binary audit proving no CLR, C++/CLI, managed assembly, managed subprocess, or wrapper dependency. · 正式切換要求每條 route、control、service、companion、launcher、updater、installer、protocol、test、文件鏡像同改過頁面截圖都通過，並有最後 binary audit 證明冇 CLR、C++/CLI、受控 assembly、受控 subprocess 或 wrapper 相依。

- [Full migration record · 完整遷移記錄](../Native-Cpp-Rewrite.md)
- [Package Manager · 套件管理](Package-Manager.md)
- [Parity ledger · 對等清單](../cpp-port-parity.json)
