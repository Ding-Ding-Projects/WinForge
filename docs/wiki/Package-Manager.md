# Package Manager · 套件管理

![Managed production Package Manager reference · 受控正式版套件管理參考](https://raw.githubusercontent.com/codingmachineedge/WinForge/main/docs/screenshot-packages.png)

> **Screenshot provenance · 截圖來源：** The large image above remains the shipping managed .NET reference and is **not native evidence**. On 2026-07-16, the required driver separately attempted `package-discover`, `package-updates`, `package-installed`, and `package-operations` at `-WaitMs 16000`; `CopyFromScreen` was unavailable and each `PrintWindow` fallback was blank or near-uniform. LowLevel MCP then created an isolated desktop, launched all four routes, and captured their resolved 1980×1320 HWNDs. Every inspected full-window frame contained only the native title bar and a blank client surface. All invalid outputs were discarded. Every changed native Package Manager view is therefore `capture-blocked`; no stale, synthetic, blank, or managed image is substituted. · **截圖來源：** 上面大圖仍然係發佈中受控 .NET 參考，**唔係原生證據**。2026-07-16，必需 driver 分別用 `-WaitMs 16000` 嘗試 `package-discover`、`package-updates`、`package-installed` 同 `package-operations`；`CopyFromScreen` 用唔到，而且每個 `PrintWindow` fallback 都係空白／接近單色。LowLevel MCP 跟住建立隔離 desktop、開啟四條 route，並擷取佢哋已解析嘅 1980×1320 HWND。每張檢查過嘅完整視窗 frame 都只得原生 title bar 同空白 client surface。全部無效輸出已丟棄。每個改過嘅原生 Package Manager view 都係 `capture-blocked`；唔會用舊、合成、空白或者受控版圖片頂替。

### Current native capture status · 目前原生截圖狀態

No native Package Manager PNG is published for this checkpoint; the only displayed image is the clearly labelled managed-production reference above. · 呢個 checkpoint 冇發佈原生 Package Manager PNG；唯一展示嘅圖片係上面清楚標示嘅受控正式版參考。

## Production and migration status · 正式版同遷移狀態

| Runtime · 執行版本 | Current truth · 目前實況 |
|---|---|
| **Shipping managed .NET app · 發佈中受控 .NET app** | The production Package Manager has substantial functionality across 11 Windows package engines and all nine workflows: discovery/install, updates, installed-package operations, bundles, sources, ignored rules, setup, settings and a shared operation queue. · 正式版套件管理器喺 11 個 Windows 套件引擎同九個流程都有大量實際功能：搜尋／安裝、更新、已安裝套件操作、清單、來源、忽略規則、引擎設定、設定同共用操作佇列。 |
| **C++20/C++/WinRT migration · C++20／C++/WinRT 遷移** | The batch runs **Discover, Updates, and Installed result queries**, read-only per-row **Details**, persisted ignore/pin/snooze rules, and a read-only **Sources** probe. A cached row can create a redacted reviewed Install/Update/Uninstall argv plan; a separate confirmation is required before serial normal-integrity execution. A source-aware cached selection of at most 25 rows, including the current eligible **Review Update all** set, now validates all commands atomically into one batch card that exposes every full redacted argv; one **Confirm batch execution** action queues them in displayed order. Cancellation stops the active command and cancels the remainder; retry returns only failed, timed-out, or cancelled children to fresh review and never replays a success. The 50-record coordinator rejects elevation/hooks/unsafe requests, custom mutation arguments, and oversized previews, retains only safe reviewed/lifecycle metadata, and withholds third-party stdout, stderr, and runtime diagnostics. This is UniGetUI-informed coverage, not a full clone, full parity claim, or production cutover. · 目前批次會執行**搜尋安裝、可更新同已安裝結果查詢**、逐列只讀**詳細資料**、保存忽略／釘選／暫停規則，同只讀**來源**探測。已快取資料列可以建立已遮蔽嘅已檢視安裝／更新／解除安裝 argv 計劃；串行正常 integrity 執行之前要另一個確認。識別來源、最多 25 條嘅快取選擇（包括目前合資格嘅**檢視全部更新**集合）而家會原子地驗證所有指令並進入一張批次卡，展示每個完整已遮蔽 argv；一個**確認批次執行**動作會按顯示次序排隊。取消會停止執行中指令並取消其餘指令；重試只會將失敗、超時或者已取消子項返回全新檢視，絕對唔會重播成功項。50 條協調器會拒絕提升權限／hooks／唔安全要求、自訂修改參數同過長預覽，只保留安全嘅已檢視／生命週期 metadata，並略去第三方 stdout、stderr 同執行時診斷。佢係參考 UniGetUI 嘅涵蓋工作，唔係完整複製、完整對等聲稱或者正式版切換。 |

The native shell exposes the same 11 manager choices: WinGet, Scoop, Chocolatey, pip, npm, .NET tools, Windows PowerShell Gallery, PowerShell 7 PSResourceGet, Cargo, Bun and vcpkg. Availability probes unlock only engines whose non-destructive version check succeeds. A visible probe or placeholder does not make the corresponding production workflow native-complete.

原生 shell 顯示同一組 11 個管理器：WinGet、Scoop、Chocolatey、pip、npm、.NET 工具、Windows PowerShell Gallery、PowerShell 7 PSResourceGet、Cargo、Bun 同 vcpkg。只有非破壞性版本探測成功嘅引擎先會解鎖。見到探測或者 placeholder 唔代表相應正式版流程已經原生完成。

### Native Discover filtering · 原生 Discover 篩選

The native Discover page now mirrors the managed cached-result contract: after a result query, it can re-filter the retained raw rows by **Both name and ID**, **Package name**, **Package ID**, **Exact match**, or **Show similar packages**, with optional case-sensitive and ignore-special-character normalization. It preserves source order and the raw cached list; choosing a mode or toggle never starts a process, network request, or another package query. New searches still require at least two non-whitespace characters. These choices are saved with the native Package Manager state when filter remembering is enabled.

原生 Discover 頁而家跟受控版已快取結果合約：結果查詢後，可以喺保留嘅原始資料列用**名稱及 ID**、**套件名稱**、**套件 ID**、**完全相同**或**顯示相似套件**重新篩選，亦可選大小寫敏感同忽略特殊字元正規化。佢會保留來源次序同原始快取清單；揀模式或者 toggle 絕對唔會開 process、網絡要求或者另一個套件查詢。新搜尋仍然要最少兩個非空白字元。開啟記住篩選時，呢啲選項會同原生套件管理狀態一齊保存。

### PCRE2 cached Discover regex · PCRE2 已快取 Discover 正規表示式

The optional PCRE2-16 mode is a separate local filter over existing cached Discover rows, never a manager-search syntax. Its pattern is never forwarded to an argv or HTTPS endpoint; while enabled, external Search is disabled and a final native guard refuses any package query. `NativePackageQueryAudit` is a monotonic runtime epoch that changes only when a real CLI/HTTPS query begins, allowing UI Automation to prove valid and invalid regex application leave the remote-query count unchanged. Exact uses full-match semantics; Similar and ignore-special normalization are unavailable in regex mode. Patterns are bounded and JIT is never used.

可選 PCRE2-16 模式係對現有已快取 Discover 資料列嘅獨立本機篩選，唔係 manager 搜尋語法。模式絕對唔會轉送去 argv 或 HTTPS endpoint；啟用時外部 Search 會停用，而且最後原生 guard 會拒絕任何套件查詢。`NativePackageQueryAudit` 係單調 runtime epoch，只有真正 CLI／HTTPS 查詢開始先會改，所以 UI Automation 可以證明有效同無效 regex 套用都唔會改遠端查詢數。Exact 用 full-match 語義；regex 模式冇 Similar 同忽略特殊字元正規化。模式有上限，而且絕對唔用 JIT。

### Native reviewed actions and batch consent · 原生已檢視動作同批次確認

For one cached Discover, Updates, or Installed row, **Review Install/Update/Uninstall** creates an `AwaitingConsent` record with its exact validated and redacted argv preview. A second **Confirm execution** action queues that exact record; the coordinator runs one command at a time with a five-minute runtime limit. Cancel before start is terminal; cancel while running sends the contained stop request; retry returns to `AwaitingConsent` and cannot run until fresh confirmation. The queue record and redacted preview remain in memory, while a bounded redacted lifecycle event is persisted in the existing Operations history. Closing or navigating away from Package Manager cancels every active/pending record. Custom mutation arguments and an oversized consent preview are refused before storage; third-party stdout/stderr/runtime diagnostics are withheld rather than displayed or persisted.

對一條已快取 Discover、Updates 或 Installed 資料列，**檢視安裝／更新／解除安裝**會用準確已驗證兼已遮蔽 argv 預覽建立 `AwaitingConsent` 記錄。第二個**確認執行**動作先會排入該條精確記錄；協調器一次只會跑一個指令，而 runtime 上限係五分鐘。開始前取消會成為終止狀態；執行中取消會發出受控停止要求；重試會返去 `AwaitingConsent`，未重新確認之前唔可以跑。佇列記錄同已遮蔽預覽留喺記憶體，而有界已遮蔽生命週期事件會保存喺現有 Operations 歷史。關閉或者離開 Package Manager 會取消全部進行中／等候記錄。自訂修改參數同過長確認預覽會喺儲存之前拒絕；第三方 stdout／stderr／執行時診斷會被略去，唔會顯示或者保存。

Source-aware, action-scoped **Review selected** accepts at most 25 cached rows. It calls the native coordinator once, so every child reference, safe action, full redacted argv, duplicate identity, and capacity rule passes before any child is retained; a failed review leaves no partial batch. **Review Update all** uses the same path for current eligible unsuppressed Updates rows. The batch card makes every preview selectable, has one explicit confirmation rather than child-level confirmation controls, queues only after that confirmation, and uses vertical controls to avoid localized action-row clipping. · 識別來源、識別動作嘅**檢視所選**最多接受 25 條快取資料列。佢只會呼叫原生協調器一次，所以每個子項參照、安全動作、完整已遮蔽 argv、重複 identity 同容量規則都會喺保存任何子項之前通過；檢視失敗唔會留下部分批次。**檢視全部更新**會對目前合資格、未隱藏嘅 Updates 資料列用同一條路徑。批次卡會令每個預覽可選取、只有一次明確確認而唔會有子項確認控制、只會喺嗰次確認之後排隊，亦會用垂直控制避免本地化動作列裁切。

### Native Bundle workspace · 原生 Bundle 工作區

Export writes through an exclusively created, cryptographically random same-directory temporary file, flushes it, and atomically replaces the destination. The workspace is capped at 2,048 records with 512-character fields; both import and export are capped at 2 MiB, and the import bound is enforced while reading the opened file handle rather than trusted pre-open metadata. · 匯出會用加密級隨機、同目錄而且獨佔建立嘅暫存檔，寫入並 flush 後原子式取代目的地。工作區上限係 2,048 筆記錄，每欄最多 512 個字元；匯入同匯出都上限 2 MiB，而匯入上限會喺讀取已開啟檔案 handle 時強制執行，唔會信任開啟前嘅 metadata。

Checked cached **Discover** and **Installed** rows can now be copied into an explicit native Bundle workspace; **Updates** remains preview-only. The workspace is metadata-only: export stays disabled while it has no record, then atomically exports bounded UniGetUI-v3-style JSON/`.ubundle` (`export_version: 3`), deduplicates compatible records by manager/source/ID/version and managerless audit records by source/ID/version, stores compatible `packages` separately from incompatible audit records, imports bounded metadata with installation-option stripping, and asks before replacing an unsaved draft. Compatible records carry `ManagerName`; native v3 incompatible audit records omit it and remain inert. It never builds or executes a package command. · 已勾嘅快取 **Discover** 同**已安裝**資料列而家可以複製入明確原生 Bundle 工作區；**Updates** 仍然只供預覽。工作區只限 metadata：冇記錄時匯出會保持停用；有記錄後會原子式匯出有上限嘅 UniGetUI v3 風格 JSON／`.ubundle`（`export_version: 3`）；相容記錄按管理器／來源／ID／版本、冇管理器稽核記錄按來源／ID／版本 identity 去重；分開相容 `packages` 同不相容審計記錄、匯入有上限 metadata 時移除安裝選項，並喺取代未儲存草稿前詢問。相容記錄有 `ManagerName`；原生 v3 不相容稽核記錄會省略佢並保持惰性。絕對唔會建立或者執行套件指令。

## Nine-view truth table · 九個檢視實況表

| View · 檢視 | Shipping managed production · 發佈中受控正式版 | Current native C++ batch · 目前原生 C++ 批次 |
|---|---|---|
| **Discover · 搜尋安裝** | Searches selected managers, filters results, views package details, and installs one or many packages. · 搜尋已選管理器、篩選結果、檢視套件詳細資料，同安裝一個或多個套件。 | Search results remain non-mutating; each row can run Details, append Bundle metadata, or create a reviewed Install plan. Up to 25 selected cached rows can be atomically reviewed as one batch with every argv visible and one later batch confirmation. · 搜尋結果仍然唔會修改系統；每列可以跑 Details、附加 Bundle metadata，或者建立已檢視安裝計劃。最多 25 條已揀快取資料列可以原子地檢視成一個批次，展示每個 argv，之後用一次批次確認。 |
| **Updates · 可更新** | Enumerates updates, views package details, updates one manager or all managers, and supports ignore, pin and snooze policies. · 列出更新、檢視套件詳細資料、逐管理器或全部更新，並支援忽略、釘選同暫停政策。 | Update enumeration remains non-mutating; a single cached row can create a reviewed Update plan. Details, Ignore, Pin, and Snooze stay local/read-only; selected rows or **Review Update all** atomically stage up to 25 current eligible commands for separate batch confirmation. · 更新列舉仍然唔會修改系統；單條已快取資料列可以建立已檢視更新計劃。Details、忽略、釘選同暫停保持本機／只讀；所選資料列或者**檢視全部更新**會原子地暫存最多 25 個目前合資格指令，之後要分開批次確認。 |
| **Installed · 已安裝** | Lists installed packages, views package details, and performs uninstall or other selected operations. · 列出已安裝套件、檢視套件詳細資料，並執行解除安裝或其他已選操作。 | Installed enumeration remains non-mutating; a single cached row can create a reviewed Uninstall plan. Bundle append stays metadata-only, while Details is read-only; up to 25 selected cached rows can form one consent-gated uninstall batch. · 已安裝套件列舉仍然唔會修改系統；單條已快取資料列可以建立已檢視解除安裝計劃。Bundle 附加保持只限 metadata，而 Details 係只讀；最多 25 條已揀快取資料列可以組成一個需要確認嘅解除安裝批次。 |
| **Bundles · 套件清單** | Builds, edits, imports, exports and installs portable package sets. · 建立、編輯、匯入、匯出同安裝可攜套件清單。 | Explicit bounded v3 metadata workspace: Discover/Installed selected rows append, compatible and incompatible audit records stay separate, bounded import strips option metadata and confirms dirty replacement, and non-empty export is atomic. YAML/XML, scripts, real installation, and broad interoperability stay gated. · 明確有上限嘅 v3 metadata 工作區：Discover／已安裝所選資料列會附加、相容同不相容審計記錄分開、有上限匯入會移除選項 metadata 並確認未儲存取代，非空白匯出係原子式。YAML／XML、script、真正安裝同廣泛互通仍然鎖住。 |
| **Sources · 來源** | Lists, adds, removes and refreshes supported sources, buckets, feeds and repositories. · 列出、新增、移除同重新整理支援嘅來源、bucket、feed 同 repository。 | Command probe only: it runs read-only source commands, then withholds all raw configuration and diagnostics until manager-specific secret redaction is proven. No source rows are shown; add/remove stays disabled. · 只限指令探測：執行只讀來源指令之後，逐管理器機密遮罩未證實之前會隱藏全部原始設定同診斷。唔會顯示來源資料列；新增／移除保持停用。 |
| **Ignored · 已忽略** | Reviews and removes version pins, all-version ignores and timed snoozes. · 檢視同移除版本釘選、全部版本忽略同限時暫停。 | Local all-version ignores, update-version pins and configurable 1/7/14/30-day snoozes are persisted in native JSON, listed in this tab, removable one-by-one, and used to hide matching Updates rows. Scheduler integration remains gated. · 本機全部版本忽略、更新版本釘選同可設定 1／7／14／30 日暫停會保存喺原生 JSON、喺呢個分頁列出、可逐項移除，並用嚟隱藏相符 Updates 資料列；排程整合仍然鎖住。 |
| **Setup · 設定引擎** | Checks availability and bootstraps missing engines or dependencies through the production coordinator. · 檢查可用性，再經正式版協調器安裝欠缺引擎或者相依。 | Non-destructive availability probes only; bootstrap install remains gated. This probe surface is not full Setup parity. · 只限非破壞性可用性探測；bootstrap 安裝仍然鎖住。探測介面唔等於完整 Setup 對等。 |
| **Settings · 設定** | Persists schedules, notifications, concurrency, manager paths/arguments, proxy, backup and install defaults. · 保存排程、通知、同時操作數、管理器路徑／參數、代理、備份同安裝預設。 | Native JSON persistence now remembers package view, search text, sort mode, ignored/pinned/snoozed update rules, manager-filter choices, and the default snooze duration; broader per-manager defaults, backup, and restore remain gated. · 原生 JSON 持久化而家會記住套件檢視、搜尋文字、排序模式、忽略／釘選／暫停更新規則、管理器篩選同預設暫停時長；更完整逐管理器預設、備份同還原仍然鎖住。 |
| **Operations · 操作佇列** | Shows active, queued and completed work with captured output, cancellation, ordering and retry. · 顯示進行中、排隊同已完成操作，附輸出、取消、次序同重試。 | The in-memory native coordinator shows individual and batch review/awaiting-consent/queued/running/terminal state. Batch cards retain every child preview and expose atomic Confirm/Cancel/Retry controls; a retry never replays successful children. It runs serially only at normal integrity, fails closed for elevation/hooks/unsafe input, withholds third-party output, and cancels work on Package Manager exit. Durable preview/query history remains a separate clearable log. · 記憶體原生協調器會顯示個別同批次嘅已檢視／等候確認／排隊／執行中／終止狀態。批次卡會保留每個子項預覽，並提供原子 Confirm／Cancel／Retry 控制；重試絕對唔會重播成功子項。只會喺正常 integrity 串行執行；提升權限／hooks／唔安全輸入會 fail closed、第三方輸出會略去，而離開 Package Manager 會取消工作。可保存預覽／查詢歷史仍然係另一個可清除 log。 |

There are therefore three enabled native result-query paths, bounded Details queries, persisted update rules, a source probe, a bounded metadata-only v3 Bundle workspace, and narrowly scoped individual and up-to-25-command batch consent coordination. Scheduler integration, Setup bootstrap, an elevation broker, broad source actions, and full per-manager parity remain gated. Settings is a focused native persistence slice; Operations separates durable preview/query history from the non-persistent consent queue.

所以目前有三條已開放原生結果查詢路徑、有界 Details 查詢、持久化更新規則、一個來源探測、有界只限 metadata 嘅 v3 Bundle 工作區，同範圍嚴格嘅個別同最多 25 指令批次確認協調。排程整合、Setup bootstrap、提升權限 broker、更廣來源動作同完整逐管理器對等仍然鎖住。Settings 係重點原生持久化批次；Operations 會分開可保存預覽／查詢歷史同唔持久嘅確認佇列。

## Shipping managed functionality · 發佈中受控功能

The following capabilities describe the **managed production implementation**, not the current native batch:

以下功能描述**受控正式版實作**，唔係目前原生批次：

- A bounded-concurrency coordinator handles row actions, multi-select batches, bundle installs and scheduled updates; it suppresses duplicate live work and retains status, output, cancellation and retry history. · 有同時操作上限嘅協調器會處理單列動作、多選批次、清單安裝同排程更新；避免重複操作，並保留狀態、輸出、取消同重試歷史。
- Global defaults and per-package overrides cover operation-specific arguments, elevation, interactive mode, hash/prerelease flags, scope, architecture, version, install location, update/uninstall policy, pre/post hooks and process shutdown rules. · 全域預設同逐套件覆寫涵蓋逐操作參數、提升權限、互動模式、雜湊／預覽版旗標、範圍、架構、版本、安裝位置、更新／解除安裝政策、前後鈎同關閉程序規則。
- Bundles support JSON, `.ubundle`, YAML and XML, preserve package options and source identity, validate references, flag incompatible entries and export PowerShell install scripts. · 清單支援 JSON、`.ubundle`、YAML 同 XML，保留套件選項同來源身份、驗證參照、標示不相容項目，亦可匯出 PowerShell 安裝腳本。
- `PackageSourcePolicy` is the managed command boundary: it emits only manager-valid source forms, preserves safe metadata where a CLI has no source selector, and rejects local, unknown, malformed or unsupported sources before preview, queueing or execution. · `PackageSourcePolicy` 係受控版指令邊界：只輸出管理器有效來源形式；CLI 冇來源選擇器時保留安全中繼資料；本機、未知、格式錯誤或者唔支援來源會喺預覽、排隊或者執行之前拒絕。
- Manager-specific behavior includes mutually exclusive `.NET --global`/`--tool-path`, version-aware PowerShell 7 cleanup, direct Bun registry search/global-manifest reads, and validated vcpkg/npm identifiers. · 各管理器保障包括互斥嘅 `.NET --global`／`--tool-path`、識別版本嘅 PowerShell 7 舊版清理、直接 Bun registry 搜尋／全域 manifest 讀取，同已驗證 vcpkg／npm ID。
- The background scheduler respects ignored/snoozed updates, minimum age, metered network, battery and Battery Saver gates, and detectable WinGet installer-host changes. · 背景排程器遵守忽略／暫停更新、最低年齡、流量計費網絡、電池同慳電模式閘，亦會檢查可偵測嘅 WinGet installer host 變更。

## Native safety and accessibility · 原生安全同無障礙

- Native queries use reviewed argument-vector builders, bounded parsers, allowlisted HTTPS endpoints and a contained process runner; no upstream UniGetUI executable or UI is launched. · 原生查詢使用經審核嘅 argument-vector builder、有界解析器、准許清單 HTTPS endpoint 同受控 process runner；完全唔會啟動上游 UniGetUI executable 或 UI。
- Details uses the same validated command builders and contained runtime as the other read-only queries, then renders safe parsed common fields above bounded command output in the result area. A cached Install, Update, or Uninstall row may enter reviewed consent; selected rows and **Review Update all** use the same coordinator only after an all-or-nothing review, never during a query. · 詳細資料會用同一套已驗證 command builder 同受控 runtime 執行，並喺結果區嘅有界指令輸出之上顯示安全已解析常用欄位。已快取安裝、更新或者解除安裝資料列可以進入已檢視確認；所選資料列同**檢視全部更新**只會喺全有或全無檢視之後用同一個協調器，絕對唔會喺查詢期間使用。
- External manager commands fail closed while WinForge is elevated. At normal integrity, an individual reviewed row needs a separate explicit confirmation, while a reviewed batch needs one separate atomic batch confirmation before the bounded serial coordinator can run it. It rejects elevation, hooks, unsafe input, custom mutation arguments, and a preview too long to review in full. Timeout and cancellation are terminal lifecycle outcomes, not validation rejections; there is still no elevation broker. · WinForge 提升權限時，外部管理器指令會 fail closed。喺正常 integrity，個別已檢視資料列要另一個明確確認，而已檢視批次要一個獨立原子批次確認，先會交畀有上限嘅串行協調器執行。提升權限、hooks、唔安全輸入、自訂修改參數同長到未能完整檢視嘅預覽都會被拒絕。逾時同取消係終止生命週期結果，唔係驗證拒絕；仍然冇提升權限 broker。
- The coordinator keeps at most 50 in-memory records, including redacted reviewed argv plus request/lifecycle metadata and numerical exit codes. It deliberately withholds raw third-party stdout, stderr, and runtime diagnostics; a bounded redacted lifecycle event is written to existing history. Leaving or closing Package Manager cancels all active/pending work. Batch capacity cleanup only removes an entire terminal batch, so a completed child stays visible if another child needs retry; retry returns only unsuccessful children to fresh consent. · 協調器最多保存 50 條記憶體記錄，包括已遮蔽已檢視 argv、要求／生命週期 metadata 同數字 exit code。佢刻意略去第三方原始 stdout、stderr 同執行時診斷；現有歷史會寫入有界已遮蔽生命週期事件。離開或者關閉 Package Manager 會取消所有進行中／等候工作。批次容量清理只會移除整個終止批次，所以有另一個子項需要重試時，完成咗嘅子項仍然可見；重試只會將未成功子項返回全新確認。
- Only successful, fully resolved parser results become current package rows. Partial PyPI candidates and unresolved .NET update rows stay out of the result list and retain an honest per-engine diagnostic. · 只有成功兼完整解析嘅結果先會成為目前套件列；部分 PyPI 候選同未解析 .NET 更新列唔會放入結果清單，並保留誠實嘅逐引擎診斷。
- Changing a manager filter, search text or view invalidates and clears the old result generation. Each manager exposes a deterministic completion state with verified row count. Ignored, pinned and snoozed update rules validate package references, persist locally, and hide matching Updates rows without launching mutation commands. · 更改管理器篩選、搜尋文字或者檢視會令舊結果 generation 失效兼清除；每個管理器都有穩定完成狀態同已驗證資料列數目。忽略、釘選同暫停更新規則會驗證套件參照、本機保存，並喺唔啟動修改指令嘅情況下隱藏相符 Updates 資料列。
- Discover search-mode and normalization toggles are deliberately different: they re-filter the existing cached raw rows locally, retain the raw cache for reversal, and announce the visible/cached count without invalidating or starting a package query. Source-aware, action-scoped multi-select remains transient across those local filter changes, but query/view invalidation clears it before any stale row can be previewed. · Discover 搜尋模式同正規化 toggle 係刻意另一回事：只會喺本機重新篩選既有快取原始資料列、保留原始快取以便還原，並宣告可見／快取數量，唔會令結果失效或者開套件查詢。識別來源、識別動作嘅多選會喺本機篩選改變時保留，但查詢／檢視失效之前一定會清除，唔會預覽舊資料列。
- A persistent bilingual live region announces probe/query start, completion, failure and invalidation using polite or assertive UI Automation notifications. · 持久雙語 live region 會用 polite 或 assertive UI Automation 通知探測／查詢開始、完成、失敗同失效。

## Evidence and current blockers · 證據同目前阻礙

**Current 2026-07-16 evidence · 2026-07-16 目前證據：** Debug and Release native tests each pass **387/387** (341 core route/package-manager checks plus 46 parser checks), including **42/42** focused Package Mutation Coordinator checks and safe cached-Discover PCRE2 regex vectors. The elevated process-owned UI Automation smoke passes **166/166**, including all eleven native manager filters, cache-only regex/no-query assertions, batch-consent policy, and horizontal-bound checks. Catalog parity passes 346 fixed routes, five dynamic families, 319 registry records, 22 categories, and 346 ledger rows. This is targeted core/UI behavior evidence only; it is not normal-integrity external-query or package-mutation execution evidence. · Debug 同 Release 原生測試各自通過 **387/387**（341 個 core route／package-manager 檢查加 46 個 parser 檢查），包括 **42/42** 個 Package Mutation Coordinator 專項檢查同安全已快取 Discover PCRE2 regex vectors。提權、只控制自己 process 嘅 UI Automation smoke 通過 **166/166**，包括全部 11 個原生管理器篩選、只限快取 regex／唔開查詢 assertion、批次確認政策同水平邊界檢查。目錄對等通過 346 條固定路線、五組動態家族、319 條 registry 記錄、22 個分類同 346 條 ledger rows。呢個只係針對 core／UI 嘅行為證據；唔代表正常 integrity 外部查詢或者套件修改執行證據。

- **Normal-integrity live external smoke:** blocked before package execution. Even an interactive scheduled task registered with `RunLevel=Limited` received a Windows token that failed the standard-user proof, so the harness stopped rather than weakening the gate. No live-query pass is claimed. · **正常權限 live 外部 smoke：** 喺執行套件之前已受阻。即使互動式排程工作登記做 `RunLevel=Limited`，Windows 回傳嘅 token 仍然未能通過標準使用者證明，所以 harness 會停止，唔會削弱安全閘；唔會聲稱 live query 已通過。
- **LowLevel MCP headless desktop:** a direct local stdio MCP session from `C:\Users\Administrator\Documents\GitHub\lowlevel-computer-use-mcp` created an isolated desktop, launched the native app, enumerated and resolved the four WinUI HWNDs, captured full-window and client-only frames, then terminated only each launched PID and closed the desktop. Every inspected PNG had a blank or near-uniform WinUI client frame. · **LowLevel MCP 無頭 desktop：** 來自 `C:\Users\Administrator\Documents\GitHub\lowlevel-computer-use-mcp` 嘅直接本機 stdio MCP session 建立隔離 desktop、開啟原生 app、列舉同解析四個 WinUI HWND、擷取完整視窗同只限 client frame，之後只會終止每個佢啟動嘅 PID 並關閉 desktop。每張檢查過嘅 PNG 都係空白／接近單色 WinUI client frame。
- **Native visual capture:** `package-discover`, `package-updates`, `package-installed`, and `package-operations` are all `capture-blocked`. The driver rejected each at `-WaitMs 16000` with `CopyFromScreen is unavailable and the PrintWindow fallback produced a blank or near-uniform WinUI client frame; graphics capture is unavailable in this desktop session.` LowLevel MCP independently resolved and captured the four isolated-desktop HWNDs; their inspected full-window frames contained a title bar plus blank client surface, so none replaces the managed reference. · **原生視覺截圖：** `package-discover`、`package-updates`、`package-installed` 同 `package-operations` 全部係 `capture-blocked`。driver 喺 `-WaitMs 16000` 用 `CopyFromScreen is unavailable and the PrintWindow fallback produced a blank or near-uniform WinUI client frame; graphics capture is unavailable in this desktop session.` 拒絕每條 route。LowLevel MCP 亦獨立解析並擷取四個隔離 desktop HWND；檢查過嘅完整視窗 frame 都係 title bar 加空白 client surface，所以冇一張可以換走受控參考。
- **Changed-page capture:** no invalid native PNG is retained; `docs/screenshot-packages.png` remains explicitly managed-production evidence and no stale, synthetic, blank, or managed substitute is used as native proof. · **改過頁面截圖：** 冇保留無效原生 PNG；`docs/screenshot-packages.png` 仍然明確係受控正式版證據，而且唔會用舊、合成、空白或者受控圖片頂替原生證明。

## Remaining UniGetUI-informed parity gaps · 尚欠 UniGetUI 參考對等項目

- Capability model, manager enablement, maintenance state and logs. · Capability 模型、管理器啟用、維護狀態同日誌。
- Manager-specific rich Details layouts/actions, scheduler/notification integration, media/share actions and open-install-location behavior. · 逐管理器豐富 Details 版面／動作、排程／通知整合、媒體／分享動作同開啟安裝位置行為。
- An elevation broker, normal-integrity live execution evidence, unified source/download tracking, and a durable full operation history. Individual and bounded batch normal-integrity execution are implemented narrowly, but current elevated-session evidence remains fail-closed. · 提升權限 broker、正常 integrity 即時執行證據、統一來源／下載追蹤，同可保存完整操作歷史。個別同有界批次正常 integrity 執行已經狹義實作，但目前提權 session 證據仍然 fail closed。
- Bundle row and bulk actions, downloads, installed detection and broader interoperability. · 清單逐列／批次動作、下載、已安裝偵測同更廣泛互通。
- Secure opt-in gates, elevation broker, and broader mutation lifecycle recovery. · 安全 opt-in 閘、提升權限 broker，同更完整修改生命週期復原。
- Backup, restore and complete per-manager settings. · 備份、還原同完整逐管理器設定。
- Actionable verb-aware notifications and tray commands. · 可操作兼識別動詞嘅通知同系統匣指令。
- Authenticated API/CLI/headless surfaces, richer deep links and widgets. · 已驗證身份嘅 API／CLI／headless 介面、更豐富 deep link 同 widget。

Linux- and macOS-only managers are outside this Windows product's scope and are not counted as WinForge parity gaps. · 只供 Linux 同 macOS 嘅管理器唔屬於呢個 Windows 產品範圍，唔會計做 WinForge 對等缺口。

## Tray shortcuts and deep links · 系統匣捷徑同深層連結

- In the managed production app, tray commands for **Discover packages**, **Updates** and **Installed packages** bring WinForge forward and select the exact view. · 喺受控正式版，系統匣嘅**搜尋安裝**、**可更新**同**已安裝**指令會帶返 WinForge 去前景，並揀返準確檢視。
- Automation can use the `package-*` and `packages-*` alias family for `discover`, `updates`, `installed`, `bundles`, `sources`, `ignored`, `setup`, `settings`, and `operations`, or `--page module.packages#bundles`; these resolve to the matching `module.packages` deep links. Native routing is present, but route availability alone is not a parity claim. · 自動化可用 `package-*` 同 `packages-*` 呢組別名去叫 `discover`、`updates`、`installed`、`bundles`、`sources`、`ignored`、`setup`、`settings` 同 `operations`，或者用 `--page module.packages#bundles`；會解析去相應嘅 `module.packages` deep link。原生導覽已存在，但淨係有 route 唔代表功能對等。

## UniGetUI provenance · UniGetUI 來源依據

The complete tracked [Devolutions/UniGetUI](https://github.com/Devolutions/UniGetUI) tree is vendored at `ThirdParty/UniGetUI`, pinned exactly to commit `21116375c8299d1db38a3c3b4c2eb7e18bc97c4e` from upstream `main` (snapshot date 2026-07-10). UniGetUI is MIT-licensed; its original notice is preserved at `ThirdParty/UniGetUI/LICENSE`, while bundled third-party material retains separate notices. `ThirdParty/**` is excluded from WinForge build and publish inputs: upstream UI/framework, IPC and telemetry are not compiled, embedded or launched. The snapshot is provenance and a behavior inventory for audited, UniGetUI-informed reimplementation—not a claim that WinForge ships UniGetUI or has cloned every feature.

完整追蹤嘅 [Devolutions/UniGetUI](https://github.com/Devolutions/UniGetUI) 原始碼樹存放喺 `ThirdParty/UniGetUI`，準確固定到上游 `main` commit `21116375c8299d1db38a3c3b4c2eb7e18bc97c4e`（快照日期 2026-07-10）。UniGetUI 採用 MIT 授權，原始 notice 保留喺 `ThirdParty/UniGetUI/LICENSE`；隨附第三方材料仍然跟返各自 notice。`ThirdParty/**` 已排除喺 WinForge 建置同發佈輸入之外；上游 UI／framework、IPC 同 telemetry 唔會被編譯、嵌入或者啟動。快照只係來源依據同行為清單，用嚟做經審核、參考 UniGetUI 嘅重新實作——唔代表 WinForge 直接發佈 UniGetUI，亦唔會聲稱已複製每項功能。

---
[← Module index · 模組索引](Home) · [README](https://github.com/codingmachineedge/WinForge/blob/main/README.md) · [Screenshots](Screenshots)
