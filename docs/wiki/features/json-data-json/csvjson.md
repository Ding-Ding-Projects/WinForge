# CSV / JSON · CSV/JSON 轉換

**EN —** Feature reference generated from the WinForge module registry, navigation map, and page XAML.
**粵語 —** 呢份功能參考由 WinForge 模組登記、導覽地圖同頁面 XAML 生成。

| Field · 欄位 | Value · 值 |
|---|---|
| Tag · 標籤 | <code>module.csvjson</code> |
| Deep-link alias · 深層連結別名 | <code>csvjson</code> |
| Category · 分類 | JSON & Data · JSON 與資料 |
| Page class · 頁面類別 | <code>CsvJsonModule</code> |
| Page XAML · 頁面 XAML | <code>Pages/CsvJsonModule.xaml</code> |
| Button docs · 按鈕文件 | 2 |

## What It Covers · 功能範圍

**EN —** CSV / JSON is registered in WinForge search and navigation with these keywords: <code>csv json convert converter parse rfc4180 delimiter table array header 轉換 逗號 表格 解析 標題列</code>.

**粵語 —** CSV/JSON 轉換 已登記喺 WinForge 搜尋同導覽，關鍵字包括：<code>csv json convert converter parse rfc4180 delimiter table array header 轉換 逗號 表格 解析 標題列</code>。

## Verification Checkpoint · 驗證 checkpoint

**EN —** On 2026-07-11, the direct self-contained
<code>WinForge.exe --page csvjson</code> launch passed after its
“first row is header” ToggleSwitch default moved from XAML into guarded managed
initialization. The current desktop capture environment remains blocked, so
this is launch evidence only, not a visual-pass claim.

**粵語 —** 2026-07-11，將「第一行係標題」ToggleSwitch 預設值由 XAML 搬去有
guard 嘅 managed initialization 後，直接 self-contained
<code>WinForge.exe --page csvjson</code> launch 通過。依家 desktop capture
環境仍然受阻，所以呢個只係 launch 證據，唔係 visual-pass 聲稱。

## Buttons And Controls · 按鈕與控制項

| Button · 按鈕 | Type · 類型 | XAML name · 名稱 | Handler · 處理函式 |
|---|---|---|---|
| [ConvertBtn](../../buttons/json-data-json/csvjson/001-convertbtn.md) | `Button` | `ConvertBtn` | `Convert_Click` |
| [CopyBtn](../../buttons/json-data-json/csvjson/002-copybtn.md) | `Button` | `CopyBtn` | `Copy_Click` |
