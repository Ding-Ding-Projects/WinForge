# Regex Cheatsheet · 正則速查

**EN —** Feature reference generated from the WinForge module registry, navigation map, and page XAML.
**粵語 —** 呢份功能參考由 WinForge 模組登記、導覽地圖同頁面 XAML 生成。

| Field · 欄位 | Value · 值 |
|---|---|
| Tag · 標籤 | <code>module.regexcheat</code> |
| Deep-link alias · 深層連結別名 | <code>regexcheat</code> |
| Category · 分類 | Dev Helpers · 開發小工具 |
| Page class · 頁面類別 | <code>RegexCheatModule</code> |
| Page XAML · 頁面 XAML | <code>Pages/RegexCheatModule.xaml</code> |
| Button docs · 按鈕文件 | 0 |

## What It Covers · 功能範圍

**EN —** Regex Cheatsheet is registered in WinForge search and navigation with these keywords: <code>regex regular expression cheatsheet reference tokens character class anchor quantifier lookaround flags .net 正則 正規表達式 速查 參考 字元類 錨點 量詞</code>.

**粵語 —** 正則速查 已登記喺 WinForge 搜尋同導覽，關鍵字包括：<code>regex regular expression cheatsheet reference tokens character class anchor quantifier lookaround flags .net 正則 正規表達式 速查 參考 字元類 錨點 量詞</code>。

## Native C++/WinRT migration status · 原生 C++/WinRT 遷移狀態

**EN —** The shipping managed page remains the behavioral oracle. The native `module.regexcheat` route is now a separate real C++/WinRT implementation backed by a pure-C++ catalog of 67 bilingual entries in nine categories and eight explicit Copy-only patterns. It defaults to literal filtering; its explicit Regex mode uses bounded local PCRE2-16, preserves prior visible entries on invalid input, and round-trips a verified pattern through the full builder. .NET-only syntax is reference text only, never executable behavior. Debug/Release native tests passed **395/395** and the isolated LowLevel MCP UI Automation shell passed **224/224**. Its inspected 852×880 full and 836×841 client frames were blank and discarded, so fresh native visual evidence is `capture-blocked`.

**粵語 —** 發佈中嘅受控版頁面仍然係行為 oracle。原生 `module.regexcheat` route 而家係獨立、真正嘅 C++/WinRT 實作，背後有純 C++ 67 項、九個分類嘅雙語目錄，同八個要明確 Copy 嘅模式。佢預設 literal 篩選；明確嘅 Regex mode 會用有界、本機 PCRE2-16，無效輸入時保留之前顯示嘅項目，並可經完整 builder round-trip 已驗證模式。.NET 專用語法只係參考文字，唔係可執行行為。Debug／Release 原生測試係 **395/395**，隔離 LowLevel MCP UI Automation shell 係 **224/224**。檢查過嘅 852×880 full 同 836×841 client frame 都係空白並已丟棄，所以最新原生 visual 證據係 `capture-blocked`。

## Buttons And Controls · 按鈕與控制項

| Button · 按鈕 | Type · 類型 | XAML name · 名稱 | Handler · 處理函式 |
|---|---|---|---|
| None detected from XAML · XAML 未偵測到 |  |  |  |
