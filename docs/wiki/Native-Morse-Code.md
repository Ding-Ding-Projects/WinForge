# Native Morse Code · 原生摩斯電碼

The native Morse Code route is documented in the canonical implementation note: [Native-Morse-Code.md](../Native-Morse-Code.md).

原生摩斯電碼 route 嘅完整實作同驗證記錄喺：[Native-Morse-Code.md](../Native-Morse-Code.md)。

It is a genuine C++/WinRT route over standard-C++ Morse logic, with `morse` and `module.morse` deep links, local-only conversion, explicit-only clipboard access, and `capture-blocked` visual evidence pending a valid headless frame.

呢個係真正嘅 C++/WinRT route，用標準 C++ Morse logic；有 `morse` 同 `module.morse` deep link、純本機轉換、只限明確 Copy 嘅剪貼簿使用，同等待有效 headless frame 嘅 `capture-blocked` 視覺證據。
