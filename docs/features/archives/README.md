# Archives workflows · 壓縮檔工作流程

This category combines generated `arc.*` quick-action records with maintained guides for the bespoke Archives page.

呢個分類將自動生成嘅 `arc.*` 快捷操作記錄，同壓縮檔專用頁面嘅維護指南放埋一齊。

## Guides · 指南

- [Safe create, filter, move, and in-archive delete](safe-create-and-delete.md) · 安全建立、篩選、搬走同檔內刪除。

## HTTP/API disposition · HTTP／API 處置

Archives invokes the local 7-Zip executable and exposes no HTTP API, so a Postman collection is not applicable. · 壓縮檔功能只會呼叫本機 7-Zip，冇 HTTP API，所以唔適用 Postman collection。
