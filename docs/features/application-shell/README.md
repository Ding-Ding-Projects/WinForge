# Application shell · 應用程式外殼

This category documents shared WinForge surfaces that remain available while users move between modules. · 呢個分類記錄用戶切換模組期間仍然可用嘅 WinForge 共用介面。

- [Notification centre](notification-centre.md) — bounded corner stack, severity-aware lifetimes, actions, and local review history. · 有界角落堆疊、按嚴重程度決定顯示時間、動作，同本機翻查記錄。

This category has no HTTP API, so a Postman collection is not applicable. · 呢個分類冇 HTTP API，所以唔適用 Postman collection。
