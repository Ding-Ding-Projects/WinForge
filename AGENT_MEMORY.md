# Persistent Agent Task Memory / 長期代理任務記憶

## Current Health Calculators controlled native integration (local gates green) / 最新健康計算器受控原生整合（本機 gate 已通過）

- `bmi`, `health`, and `module.bmi` are now integrated into the C++/WinRT shell over the standard-C++ `Bmi` core. Preserve WHO BMI bands, Mifflin–St Jeor BMR, all five TDEE factors, US Navy male/female body-fat rules, raw metric/imperial relabelling, invalid recovery, three language modes, reset-on-reentry, and no clipboard write path. Controlled combined proof is Debug and Release x64 native solution builds with 0 errors, both core suites **842/842** (BMI **14/14**), focused BMI UIA **14/14**, catalog parity **346 + 5** / 319 registry records / 346 ledger rows, and the native installer contract. Renderer accounting is **35/346 fixed routes** (**35 `in-progress` / 311 `not-started`**). The local LowLevel checkout is present but no callable tool is exposed; the required driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, retained no PNG, and visual remains `capture-blocked`. No release workflow or managed-release publisher changed. · `bmi`、`health` 同 `module.bmi` 而家已同 C++/WinRT shell 受控整合，背後係標準 C++ `Bmi` core。保留 WHO BMI 分級、Mifflin–St Jeor BMR、五個 TDEE 系數、美國海軍男女體脂規則、原始公制／英制重標籤、無效輸入復原、三種語言、重新入 route 時重設，同埋冇寫剪貼簿路徑。合併本機證據係 Debug／Release x64 solution 0 errors、core 各 **842/842**（BMI **14/14**）、BMI 專項 UIA **14/14**、catalog parity **346 + 5**／319 registry／346 ledger 同 native installer contract。renderer 計 **35/346**，ledger **35 `in-progress` / 311 `not-started`**。本機 LowLevel checkout 喺度但冇可呼叫工具；driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，冇 PNG，visual 保持 `capture-blocked`。冇改 release workflow 或 managed-release publisher。

## Current Unit Price controlled native integration (local gates green) / 最新單位價格受控原生整合（本機 gate 已通過）

- `priceper`, `unitprice`, and `module.unitprice` are now integrated into the C++/WinRT shell over the standard-C++ `UnitPrice` core. The controlled combined proof is Debug and Release x64 native solution builds with 0 errors, both core suites **828/828** (Unit Price **13/13**), focused Unit Price UIA **15/15**, Utility UIA **39/39** including CSS Unit Converter, catalog parity **346 + 5** / 319 registry records / 346 ledger rows, and the native installer contract. Renderer accounting is **34/346 fixed routes** (**34 `in-progress` / 312 `not-started`**). A broad aggregate exercised the Unit Price block but did not yield a captured final footer, so no completed full-shell result is claimed. The local LowLevel checkout is present but no callable tool is exposed; the fresh driver rejected its blank/near-uniform fallback after `CopyFromScreen` was unavailable, retained no PNG, and visual remains `capture-blocked`. No release workflow or managed-release publisher changed. · `priceper`、`unitprice` 同 `module.unitprice` 而家已同 C++/WinRT shell 受控整合，背後係標準 C++ `UnitPrice` core。合併本機證據係 Debug／Release x64 solution 0 errors、core 各 **828/828**（Unit Price **13/13**）、Unit Price 專項 UIA **15/15**、包括 CSS Unit Converter 嘅 Utility UIA **39/39**、catalog parity **346 + 5**／319 registry／346 ledger 同 native installer contract。renderer 計 **34/346**，ledger **34 `in-progress` / 312 `not-started`**。廣泛 aggregate 已跑過 Unit Price block，但冇擷取到最後 footer，所以唔聲稱完成 full shell。本機 LowLevel checkout 喺度但冇可呼叫工具；driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，冇 PNG，visual 保持 `capture-blocked`。冇改 release workflow 或 managed-release publisher。

- Remote proof: controlled Unit Price merge `37cc0e8a1d4605864756751265d379a954978b27` is an ancestor of `origin/main`. Hosted [native run 29706847786](https://github.com/codingmachineedge/WinForge/actions/runs/29706847786) passed every native build, core-test, parity, installer, exact-asset, and publish gate and created stable `native-v1.0.76` at that exact SHA with only `WinForge-Native-Setup.exe` and `WinForge-native-x64-1.0.76.zip`. Successful site-data [run 29706847769](https://github.com/codingmachineedge/WinForge/actions/runs/29706847769) committed `fbaff0788`; its dispatched [native run 29707001900](https://github.com/codingmachineedge/WinForge/actions/runs/29707001900) passed and created current Latest `native-v1.0.77` at that exact current-main SHA, again with exactly the native setup and ZIP. · 遙距證明：Unit Price merge `37cc0e8a1d4605864756751265d379a954978b27` 係 `origin/main` 祖先。Hosted native run 29706847786 通過所有原生 build、core test、parity、installer、asset 同 publish gate，準確喺嗰個 SHA 出 stable `native-v1.0.76`，只得 native setup 同版本化 ZIP。成功 site-data run 29706847769 提交 `fbaff0788`；佢 dispatch 嘅 native run 29707001900 通過，準確喺 current-main SHA 出 Latest `native-v1.0.77`，仍然只有原生 setup 同 ZIP。

## Current Namespaced UUID controlled native integration (local gates green) / 最新具名空間 UUID 受控原生整合（本機 gate 已通過）

- `uuid5`, `uuidv5`, and `module.uuidv5` are merged with the existing native renderer set, backed by the standard-C++ `UuidV5` core. Preserve RFC 4122 network-order v3/MD5 and v5/SHA-1 behavior, managed D/N/B/P/X parsing, malformed-UTF-16 replacement, U+180E whitespace parity, local bulk rows, three language modes, reset-on-reentry, and explicit-only clipboard writes. Final controlled-merge proof is Debug/Release x64 solution builds with 0 errors, both cores **815/815**, focused UUID UIA **21/21**, catalog parity **346 + 5** / 319 registry records / 346 ledger rows, installer contract pass, and full native shell **469/469**. Renderer accounting is **33/346**, **33 `in-progress` / 313 `not-started`**. The local LowLevel checkout is present but no callable tool is exposed; the fresh UUID driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, so no PNG is retained and visual remains `capture-blocked`. · `uuid5`、`uuidv5` 同 `module.uuidv5` 已同現有 native renderer set 整合，背後係標準 C++ `UuidV5` core。要保留 RFC 4122 network-order v3/MD5 同 v5/SHA-1、managed D/N/B/P/X parser、無效 UTF-16 replacement、U+180E 空白 parity、本機 bulk 行、三種語言、重新入 route 時重設，同埋只限明確 Copy 寫剪貼簿。最終受控 merge 證據係 Debug／Release x64 solution 0 errors、core 各 **815/815**、UUID 專項 UIA **21/21**、catalog parity **346 + 5**／319 registry／346 ledger、installer contract 同完整 native shell **469/469** 通過。renderer 計 **33/346**，ledger **33 `in-progress` / 313 `not-started`**。本機 LowLevel checkout 喺度但冇可呼叫工具；最新 UUID driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，所以冇 PNG、visual 保持 `capture-blocked`。

## Historical Unit Price feature-branch checkpoint (pre-integration) / 歷史原生單位價格功能分支檢查點（整合前）

- `priceper`, `unitprice`, and `module.unitprice` now have a genuine C++/WinRT renderer over a pure standard-C++ `UnitPrice` core. Preserve managed valid-row filtering, free/infinity and tolerance-tie behavior, invariant display text, first-unit Add, removal/lifecycle reset, three language modes, and explicit-only Copy. Branch-local evidence is Debug/Release x64 0-error builds, core **814/814** in both configurations including Unit Price **13/13**, catalog parity **346 + 5**, and focused UIA **15/15**; renderer accounting is **33/346**, **33 `in-progress` / 313 `not-started`**. · `priceper`、`unitprice` 同 `module.unitprice` 而家係真正 C++/WinRT renderer 加純標準 C++ `UnitPrice` core。保留 managed 有效行篩選、免費／infinity／容差平手、invariant 顯示、第一行 Add、移除／lifecycle reset、三種語言同只限明確 Copy。分支本機證據係 Debug／Release 0 errors、core 各 **814/814**（Unit Price **13/13**）、catalog parity **346 + 5** 同專項 UIA **15/15**；renderer 係 **33/346**。
- The one broad native shell attempt passed its entire Unit Price block then stalled later in unrelated CSS Unit Converter work; the integration owner directed exact owned-process cleanup. Do not describe it as full-shell evidence. The LowLevel checkout is present but not callable; the required driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, retained no PNG, and visual stays `capture-blocked`. This feature branch changes no `.github` workflow or release policy, is rooted at `ef822c1dc64a30662e2a48af998d6f0387422667`, and must remain unpushed until the C++-only release owner authorizes controlled integration. · 唔好將嗰次廣泛 shell 當 full-shell 證據；LowLevel MCP 不可呼叫、driver 拒絕空白 fallback、冇 PNG，visual 保持 `capture-blocked`。分支唔改 `.github`／release policy，未經只限 C++ release owner 授權前唔可以 push。

## Latest Slugify controlled native integration (local validation green) / 最新網址別名受控原生整合（本機驗證通過）

- Slugify (`slug`, `slugify`, `module.slugify`) is integrated with native Reference Text and Morse without changing the C++-only release policy. Preserve its standard-C++ managed-compatible line/diacritic/Unicode/separator/case/UTF-16/preview semantics, route reset, three language modes, and explicit-only Copy. The combined evidence is Debug/Release x64 0-error builds, **801/801** core in both configurations, catalog parity **346 + 5**, focused UIA **29/29** Reference Text, **13/13** Morse, **12/12** Slugify, and full native shell **441/441**. Renderer accounting is **32/346**, **32 `in-progress` / 314 `not-started`**. The local LowLevel checkout is present but no callable tool is exposed; the native Slugify driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, so no PNG is retained and visual remains `capture-blocked`. · Slugify 三個 alias 已同原生 Reference Text 同 Morse 整合，唔改只限 C++ 嘅發佈政策。保留標準 C++ managed 相容分行／重音／Unicode／分隔符／大小寫／UTF-16／預覽、route 重設、三種語言同只限明確 Copy。合併證據係 Debug／Release 0 errors、core 各 **801/801**、catalog parity **346 + 5**、Reference Text **29/29**、Morse **13/13**、Slugify **12/12** 同完整 shell **441/441**；renderer 計 **32/346**，ledger **32 `in-progress` / 314 `not-started`**。本機 LowLevel checkout 喺度但冇可呼叫工具；Slugify driver 因 `CopyFromScreen` 不可用而拒絕空白／近乎單色 fallback，所以冇 PNG、visual 保持 `capture-blocked`。

- Remote proof: Slugify tip `bb853ef3` and merge `88672704` are ancestors of `origin/main`. Native [run 29702620758](https://github.com/codingmachineedge/WinForge/actions/runs/29702620758) passed and produced non-draft `native-v1.0.70` exactly at `88672704`, with only `WinForge-Native-Setup.exe` and `WinForge-native-x64-1.0.70.zip`. The successful [site-data run 29702620742](https://github.com/codingmachineedge/WinForge/actions/runs/29702620742) committed `ef822c1d`; dispatched [run 29702777841](https://github.com/codingmachineedge/WinForge/actions/runs/29702777841) passed and made stable `native-v1.0.71` Latest at that exact current-main SHA, again with exactly the native setup and ZIP. · 遙距證明：Slugify tip `bb853ef3` 同 merge `88672704` 都係 `origin/main` 祖先。原生 run 29702620758 通過，準確喺 `88672704` 產生非 draft `native-v1.0.70`，只得 native setup 同 `WinForge-native-x64-1.0.70.zip`。成功 site-data run 29702620742 提交 `ef822c1d`；dispatch run 29702777841 通過，將準確 current-main SHA 嘅 stable `native-v1.0.71` 設為 Latest，仍然只得原生 setup 同 ZIP。

## Current 2026-07-19 native Base Converter controlled integration — local gates green / 2026-07-19 原生進位轉換受控整合 — 本機 gate 已通過

- `baseconvert` and `module.baseconvert` are integrated as a dedicated C++/WinRT renderer over dependency-free standard-C++ arbitrary-precision conversion. Preserve managed base 2–36 parsing/rendering, signed/grouped/64-bit presentation, BigInteger-style AND/OR/XOR/NAND/NOR/arithmetic-shift semantics, fresh-route reset, language rerender retention, accessibility, explicit-only clipboard writes, and managed Unicode `Trim()` text in invalid-input diagnostics. · `baseconvert` 同 `module.baseconvert` 已整合成專用 C++/WinRT renderer 同唔靠依賴嘅標準 C++ 任意精度轉換；要保留 managed 2–36 解析／輸出、有符號／分組／64-bit 顯示、BigInteger 式 AND／OR／XOR／NAND／NOR／arithmetic shift、fresh-route 重設、轉語言保留、無障礙、只限明確剪貼簿寫入，同無效輸入診斷嘅 managed Unicode `Trim()`。
- Controlled evidence: Debug/Release x64 solution builds exit 0 with 0 errors; Debug/Release combined core suites are **857/857**, including **15/15** Base Converter contracts; focused `-BaseConvertRoutesOnly -AllowClipboardMutation` UIA is **14/14**; catalog parity is 346+5 with 319 registry and 346 ledger rows; the installer contract passes; and renderer accounting is **36/346** (`36 in-progress / 310 not-started`). The local LowLevel MCP checkout exists but its headless tools are not callable in this session. The fresh current driver reported CopyFromScreen unavailable, rejected its blank/near-uniform PrintWindow client, retained no PNG, and left no WinForge process. Visual evidence is `capture-blocked`, not visual-pass. · 受控證據：Debug／Release 0 errors、合併 core 各 **857/857**（Base Converter **15/15**）、`-BaseConvertRoutesOnly -AllowClipboardMutation` UIA **14/14**、catalog parity 346+5（319 registry／346 ledger）、installer contract 通過，renderer 係 **36/346**（`36 in-progress / 310 not-started`）。本機 LowLevel MCP checkout 喺度但今個 session 頭less 工具不可呼叫。最新目前 driver 報 CopyFromScreen 不可用、拒絕空白／近乎單色 PrintWindow client、冇留 PNG、亦冇 WinForge process；visual 係 `capture-blocked`，唔係 visual-pass。
- Preserve the boundary: no `.github` or release-policy change is included; commit, push, and verify the existing C++-only release flow before the next feature push. · 要守住界線：冇包含 `.github` 或 release-policy 改動；下一個功能 push 前要先 commit、push 同驗證現有只限 C++ release flow。

## Historical combined Reference Text + Morse Code integration (hosted release proven) / 歷史參考文字及摩斯電碼整合（hosted 發佈已證明）

- `module.morse` and `morse` now render natively through C++/WinRT over the standard-C++ Morse core. Preserve managed-compatible UTF-16 encode/decode aliases, unique unknown-symbol reporting, exact 1/3/7-unit flash timeline and bounded WPM conversion, explicit-only clipboard writes, and dispatcher-timer cleanup on completion, route exit, or window close. Reference Text continues to provide native Phonetic Speller, Box & Banner Text, and HTML Entities renderers.
- Controlled merge validation over current main plus `6d9c4ae8f25726cf7d17549310f34b33194d669f` is green: Debug and Release solution builds exit 0 with 0 errors; both core executables pass **783/783**; focused Reference Text UI Automation passes **29/29**; focused Morse UI Automation passes **13/13**; catalog parity covers **346 fixed routes + five dynamic families**; and the exhaustive native shell passes **430/430**. The ledger is **31 `in-progress` / 315 `not-started`**; no pending or capture-blocked renderer is called complete.
- LowLevel MCP is available on disk but not callable in this Codex session. The required native `morse` driver launch rejected screenshot fallback because `CopyFromScreen` is unavailable and `PrintWindow` was blank or near-uniform. Keep visual evidence `capture-blocked`; retain no invalid PNG and do not claim a screenshot.
- Source commit `66bb415e5f1730bb128f4d649e493150da222d06`, tip `6d9c4ae8f25726cf7d17549310f34b33194d669f`, Reference Text `25cc6a78f7b0504aa4f071176ec526baf2fc01e5`, and merge `dc95f0f83390e370ecd22f3e239261b8bd2ead94` are all ancestors of `origin/main`. Hosted [native run 29700338773](https://github.com/codingmachineedge/WinForge/actions/runs/29700338773) passed every native build/test/parity/installer/release gate and published stable [`native-v1.0.64`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.64) at the merge SHA with exactly the native setup and ZIP. The paired site-data run [29700338741](https://github.com/codingmachineedge/WinForge/actions/runs/29700338741) committed generated data as `5e2636fdb84fcb691f3cd272e5dd9231cc427e21`; its successful [native run 29700486762](https://github.com/codingmachineedge/WinForge/actions/runs/29700486762) published stable [`native-v1.0.65`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.65), now `/releases/latest`, at that current main SHA. Downloaded v1.0.65 setup/ZIP SHA-256 values match GitHub (`1c380f1c…25ed`, `f458c8ac…31f7`); the ZIP has 292 entries, one MZ `WinForge.exe`, and no `WinForge.dll`, `.deps.json`, or `.runtimeconfig.json` payload. `.github/workflows/native-release.yml` remains the sole publisher.

This file is the durable execution contract for every agent working in this repository. It supplements, and never weakens, `AGENTS.md`.

## Historical feature-only Slugify branch checkpoint (2026-07-19) / 歷史只限功能網址別名分支檢查點（2026-07-19）

- Native Slugify (`slug`, `slugify`, `module.slugify`) now has a dedicated C++/WinRT renderer backed by standard-C++ `Slugify`. It preserves managed line boundaries, blank-line skipping, accent and explicit Unicode behavior, separator/case/UTF-16-length rules, first-line preview, fresh-route reset, localization state retention, and explicit-only Copy. Debug/Release builds exit 0 with 0 errors; both core executables pass **777/777**, including Slugify **18/18**; catalog parity covers 346 fixed routes plus five dynamic families; and focused `-SlugifyRoutesOnly -AllowClipboardMutation` passes **12/12**. The LowLevel MCP tools are not callable in this session; the required driver rejected a blank/near-uniform fallback after `CopyFromScreen` was unavailable, left no PNG/process, and the ledger row remains `in-progress` / `capture-blocked`. This is a feature-only `codex/native-slugify` handoff: do not mistake it for a main/release integration or modify its release-policy boundary. · 原生網址別名三個 alias 而家有專用 C++/WinRT renderer 同標準 C++ `Slugify`；Debug／Release 0 errors、core 各 **777/777**（Slugify **18/18**）、catalog parity 346+5、專項 UIA **12/12**。今次冇可呼叫 LowLevel MCP，driver 拒絕空白 fallback，冇 PNG／冇 process；ledger 保持 `in-progress`／`capture-blocked`。呢個只係 `codex/native-slugify` 功能交接，唔係 main／release 整合，唔好改佢嘅 release-policy 界線。
- Native Phonetic Speller, Box & Banner Text, and HTML Entities have dedicated C++/WinRT renderers backed by standard-C++ `ReferenceText`. They preserve the managed alphabets, UTF-16 row behavior, eight box/comment styles, Unicode-aware layout, named/numeric entity handling, invalid-UTF-16 safety, and 50-row bilingual reference; all are local-only and Copy is explicit-only.
- Commit `c6f8a24d52e4949596efd58e070e0601a2939511` remains the Text Analysis collation checkpoint: its shared ICU collator follows .NET width/kana tailoring, preserves canonical-equivalent order, avoids comparator serialization and per-edit polite announcements, and carries the required attribution in `THIRD-PARTY-NOTICES.txt`.
- Fresh controlled integration evidence is green: Debug/Release x64 builds have 0 errors, core is **759/759** in each configuration, catalog parity covers 346 fixed routes plus five dynamic families, focused `-ReferenceTextRoutesOnly` UI Automation is **29/29** across eight aliases, and the full native shell is **417/417 (417 passed, 0 failed)**. The earlier c6 snapshot remains **717/717** core (Text Analysis **87/87**), **40/40** focused UIA, and **388/388** shell.
- `HasNativeRenderer` and the ledger now account for **30/346 fixed routes**, leaving **316** plus five dynamic families: exactly **30 `in-progress` / 316 `not-started`**. A pending renderer is never called complete.
- LowLevel Computer Use MCP is not callable in this Codex session. The fresh `wordfreq` and `htmlentities` driver attempts rejected blank/near-uniform `PrintWindow` fallbacks because `CopyFromScreen` was unavailable; no PNG was retained or substituted, no canonical screenshot changed, and affected rows remain `capture-blocked`.
- Historical hosted release proof: `03b1e66f21b07c28fe2bd0b30ef60fe4af134e59` passed [main run 29695569334](https://github.com/codingmachineedge/WinForge/actions/runs/29695569334) and published stable, non-draft [`native-v1.0.57`](https://github.com/codingmachineedge/WinForge/releases/tag/native-v1.0.57) at that SHA with exactly `WinForge-Native-Setup.exe` and `WinForge-native-x64-1.0.57.zip`; [site-data run 29695569335](https://github.com/codingmachineedge/WinForge/actions/runs/29695569335) staged first and logged `No change to design/winforge-data.js.` `.github/workflows/native-release.yml` remains the sole publisher.
- 原生拼讀字母表、文字方框／橫幅同 HTML 實體而家有專用 C++/WinRT renderer，同時共用標準 C++ `ReferenceText`；保留受控版字母表、UTF-16 列、八種框／註解風格、Unicode 排版、實體處理、無效 UTF-16 安全同 50 項雙語參考，而且只會喺明確 Copy 後改剪貼簿。`c6f8a24d` 仍然係 Text Analysis 排序檢查點：ICU 跟 .NET 闊度／假名 tailoring、保留 canonical-equivalent 次序、唔會每次比較序列化或者每次修改做 polite announcement。最新受控整合證據係 Debug／Release 0 errors、core 各 **759/759**、catalog parity、八個 alias UIA **29/29** 同完整 native shell **417/417（417 passed、0 failed）**。c6 較早快照係 core **717/717**（Text Analysis **87/87**）、UIA **40/40**、shell **388/388**。renderer 而家係 **30/346**，仲有 **316** 條同五組家族，ledger 保持 **30 `in-progress` / 316 `not-started`**。今次 session 冇可呼叫 LowLevel MCP；`wordfreq` 同整合後最新 `htmlentities` driver 都拒絕空白 fallback，冇 PNG／冇 canonical 截圖變更，所以 visual 保持 `capture-blocked`。歷史 hosted `native-v1.0.57` 證明只有原生兩個 asset，而 `.github/workflows/native-release.yml` 仍係唯一 publisher。

## Earlier native line-processing checkpoint (focused validation green; visual capture blocked) / 較早原生行文字處理檢查點（專項驗證通過；視覺擷取受阻）

- The managed build's 161 warnings are historical, non-contract output; only exit 0 with 0 errors is the durable gate. · Managed build 嘅 161 個 warning 只係歷史、非合約輸出；長期 gate 只係 exit 0 同 0 errors。
- Native source now contains dedicated C++/WinRT renderers for Line Tools, Line Sort & Dedupe, and Text Wrap, backed by the shared standard-C++ `LineProcessing` core. `HasNativeRenderer` and the provisional parity ledger account for **24/346 fixed routes**, leaving **322 fixed routes** plus five dynamic route families.
- Native Debug and Release builds each exit 0. Both core executables pass **630/630**, including **70/70** focused Line Processing contracts across managed-compatible line splitting/counting, Unicode cleanup and ordinal folding, CoreLib-style sorting, natural order, deterministic shuffle fixtures, paragraph wrap/reflow, and UTF-16 metrics. Catalog parity passes all 346 fixed routes plus five dynamic families. The managed Debug x64 solution build exits 0 with 0 errors; that observed run reported 161 existing warnings.
- Focused `-LineRoutesOnly` UI Automation passes **42/42** across all seven registered aliases, and the full owned native shell smoke passes **348/348** with exit 0. LowLevel Computer Use MCP 1.28.1 launched the exact canonical entry commands `--page lines`, `--page textsort`, and `--page textwrap` on separate private desktops and resolved one **852×880** window per launch. Its `PrintWindow` captures returned `rendered_ok` but were composition-white and invalid when inspected. The compositor-backed retry failed `show_headless_desktop` with Win32 error 5 before monitor capture, and the required run-winforge driver rejected blank/near-uniform fallbacks for all three routes. Exact processes/desktops were cleaned, all invalid images were deleted, and the artifact directory is empty. Keep the three parity rows `in-progress` with visual evidence `capture-blocked`; only hosted release-per-push proof, commit integration, and remote proof remain pending.
- 原生 source 而家有行工具、行排序同去重、文字換行三個專用 C++/WinRT renderer，共用標準 C++ `LineProcessing` core；Debug／Release build 各自 exit 0，兩個 core executable 各 **630/630**，包括 Line Processing **70/70**；catalog parity 亦通過 346 條固定 route 加五組動態家族。Managed Debug x64 solution build exit 0、0 errors，161 個 warning 只係嗰次實際 run 嘅歷史、非合約輸出。`-LineRoutesOnly` 專項 UIA 喺全部七個 registered alias 通過 **42/42**，完整自有原生 shell smoke 亦以 exit 0 通過 **348/348**；LowLevel MCP 1.28.1 喺獨立 private desktop 準確開過 `--page lines`、`--page textsort` 同 `--page textwrap`，每次都解析到 **852×880** window；但 `PrintWindow` 圖雖然回報 `rendered_ok`，檢查後係 composition-white 無效畫面。Compositor retry 因 Win32 錯誤 5 失敗，run-winforge 三頁亦拒絕空白 fallback；全部 process／desktop／無效圖已清理、artifact directory 已清空，所以 visual 保持 `capture-blocked`。只剩 release 同 Git 遙距證明待完成。

## Earlier native utility checkpoint / 較早原生工具檢查點

- Commits `828c32791e1f135d2a46848e9283087ef8ec9156` and `ce879cc6626eae328ec72e0143761c0edfbae340` deliver genuine native Text Diff, Aspect Ratio, and CSS Unit Converter routes. Local `main` was fast-forwarded through the hardened tip before the final handoff record.
- Native Debug/Release core is **560/560** each; Design Tools is **94/94**, Text Diff is **27/27**, focused utility UI Automation is **39/39**, full owned shell is **300/300**, and catalog parity covers 346 fixed routes plus five dynamic families. The renderer ledger is **21 in-progress / 325 not-started** because compositor capture remains the only incomplete evidence dimension.
- LowLevel Computer Use MCP 1.28.1 launched all three pages on separate named headless desktops from an immutable 294-file stage, confirmed launch PIDs, resolved 1320×880 WinUI frames, captured/inspected 1304×841 clients, and performed exact cleanup. Each client was uniformly white; the repository driver independently rejected the same blank fallback. No blank image was promoted.
- Hosted run 29663954724 published exact-SHA prerelease `native-v1.0.37` with installer and portable zip assets for `ce879cc6626eae328ec72e0143761c0edfbae340`.
- 呢批真正原生化文字差異比對、長寬比計算同 CSS 單位換算；功能證據全綠，視覺只因 compositor 無法擷取而保持 `capture-blocked`。完成後仍有 325 條固定 route 加五組動態家族要繼續。

## Completion and Git / 完成與 Git

- Treat every bounded task as incomplete until its intentional bilingual commit has been pushed.
- Complete each task on a temporary `codex/` branch, merge it into `main`, and push `main` before calling it finished.
- After pushing `main`, fetch the remote and prove both the task commit and branch tip are ancestors of `origin/main`; also verify the expected changed files exist in the remote `main` tree.
- Only after that proof may an agent delete the merged remote/local branch or its worktree. Never delete unmerged, unpushed, or unverified work.
- Never force-push and never discard unrelated user changes.

## Documentation and evidence / 文件與證據

- A feature or page change includes its documentation work: update the relevant `README.md`, `docs/wiki/`, `docs/`, and GitHub Pages content under `design/content/wiki/`, keeping English and Cantonese aligned with the shipped UI.
- For visual changes, capture and inspect current, high-detail evidence for every changed page; replace stale canonical screenshots and matching wiki/Pages assets. If capture is blocked, record the exact blocker and do not claim visual verification.
- Generated documentation remains generated: use its repository generator instead of hand-editing generated feature/reference artifacts.

## LowLevel headless operation / LowLevel 無頭運作

- Use the inexpensive LowLevel Computer Use MCP runner for task commands and all app interaction.
- Launch and drive WinForge only in a dedicated headless desktop. Do not open a visible instance that can steal the user's focus.
- Capture UI evidence through LowLevel MCP from that headless desktop, inspect it, and close test processes/desktops when their evidence is complete.

## Security and hygiene / 安全與整潔

- Never persist, log, copy, or screenshot secrets unnecessarily.
- Keep the task focused; preserve unrelated work already present in the worktree.
- Record verification results honestly, including failures and recovery steps.
